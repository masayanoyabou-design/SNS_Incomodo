# Incomodo 作業マニュアル

## このマニュアルについて

- **最優先の目的**：作業者（人間）が、各作業の「目的」を理解したうえで、同じ作業を再現できること
- **副次的な目的**：AIがこのマニュアルを読んだときに、現在の状態（IDやURLなどの具体的な値）を把握し、次の作業を補助・代行できること
- **更新方針**：新しい作業を行うたびに、このファイルに追記する。過去の記述は基本的に消さず、状態が変わった場合は「現在の値」を更新する
- 進捗の一覧・チェックリストは `progress.html`、作業結果の要約は `README.md` を参照。このファイルは「なぜ・どうやって」を記録する場所

---

## プロジェクトの現在地（要点）

| 項目 | 値 |
|---|---|
| LP公開URL | https://snazzy-parfait-172aba.netlify.app |
| Netlifyデプロイ方法 | 匿名Drop → アカウントにclaim済み → Public設定 |
| 事前登録データの保存先 | Googleフォーム（フォームID: `1FAIpQLSdnMjpCoDwsd4gjBwgpzCEqGPw0SBKWqhOaSx0ZL_3fdaFAXw`） |
| Googleフォームのメール欄entry ID | `entry.50285302` |
| Metaピクセル ID（正） | `1457252082902363`（ビジネスポートフォリオ「incomodo.app」に属する。広告アカウントで選択できるのはこちらのみ） |
| Metaピクセル ID（旧・使用しない） | `1714208769855921`（別の場所で作成されてしまった無関係のピクセル。ビジネス設定のデータセット一覧には出てこない） |
| プライバシーポリシー連絡先 | incomodo.app@gmail.com |
| 運営者名 | 後藤雅也（個人） |
| 需要検証の予算 | ¥10,000 |
| 目標CPA | ¥150〜300（想定登録33〜66件） |
| 需要検証の実績 | ¥10,000で37件登録・CPA約¥270（2026/09/03〜09/10配信） → **Go判定**（詳細は9章） |
| Flutter SDK | 3.47.3（`C:\dev\flutter`、OneDrive同期対象外） |
| Android SDK | `C:\dev\android-sdk`（同期対象外）。Platform 36・Build-Tools 36.1.0 |
| Firebaseプロジェクト用アカウント | incomodo.app@gmail.com |
| Firebaseプロジェクト | `incomodo-c161e`（表示名: Incomodo） |
| Flutterアプリのパッケージ/バンドルID | `app.incomodo.incomodo`（Android/iOS共通、Firebaseにも同IDで登録済み） |
| Firestore | 東京リージョン（`asia-northeast1`、**変更不可**）、削除保護あり。ルールは`app/firestore.rules` |
| Googleログイン用 Web クライアントID | `227856433742-ou8hlre73l6209895a5vkkjqltcc2rq7.apps.googleusercontent.com`（`auth_service.dart`の`serverClientId`） |
| テスト用エミュレータ | `Incomodo_Play`（Pixel 7・Android 16・Play Store入り、Googleアカウント追加済み） |

---

## 1. LP（ランディングページ）本体の構築

### 目的
Meta広告で需要検証（スモークテスト）を行うための、軽量かつ単一HTMLファイルで完結するLPを作る。広告経由の訪問者の離脱を防ぐため、読み込み速度を優先する。

### やったこと
- `index.html` に、ヒーロー・課題提起・3つの特徴・アプリ画面モックアップ・事前登録フォームを実装
- クラフト紙・アナログをテーマにしたデザインシステム（配色・フォント：Noto Serif JP / Special Elite）
- スクロール連動のフェードインアニメーション、フォーム送信時の疑似モーダル

### 得られた成果物
- `index.html`（LP本体）
- `assets/`（アプリ画面モックアップ画像3枚：shot-map.jpg, shot-letter.jpg, shot-send.jpg）

### ハマりどころ・注意点（コードを触るAI向け）
- **Chromeのレンダリングバグ**：`justify-content: space-between` を使ったflexコンテナ内に、テキストと`<svg>`が兄弟要素として存在すると、SVGが描画されないことがある（headlessでもheadedでも再現）。回避策：`gap`プロパティで間隔を作る、または`margin-left:auto`ではなく単純なflex-startレイアウトにする。**新しくSVGアイコンをflexレイアウト内に追加する際は要注意。**
- 画像を`<img>`タグで使う場合、ファイル名に日本語を使うと将来的なホスティング先によってはURLエンコードの問題が起きうる。現在の`shot-*.jpg`は英数字名にして回避済み。

---

## 2. プライバシーポリシーの作成

### 目的
メールアドレス（個人情報）を収集する以上、個人情報保護法上の観点から必要。また、Meta広告の審査を通すためにも実質的に必須。

### やったこと
- `privacy.html` を新規作成し、LPフッターからリンク
- 運営者情報：法人ではなく個人（後藤雅也）として記載。所在地は「ご請求があれば遅滞なく開示いたします」として、住所は非公開のままでよい形にした（個人情報保護法上、住所公開は必須ではないため）
- Cookie/計測ツールの章に、実際に導入したMetaピクセルを明記

### 判断の理由（AIが将来この方針を変える際に参照）
- **特定商取引法（住所公開が必須）は「有償で物/サービスを売る」場合のみ適用される**。現在の事前登録は無料のため対象外。切手課金（マネタイズ）を開始する時点で別途対応が必要（バックログ B16参照）
- 副業・個人事業主登録なしでも、個人名義でプライバシーポリシーを公開して問題ない

### 得られた成果物
- `privacy.html`

---

## 3. 事前登録データの受け皿（Googleフォーム連携）

### 目的
LPのフォームが送信演出だけで実データを保存していなかった問題を解消する。かつ、これまで作り込んだLP独自のデザイン・モーダル演出を壊したくない。

### 検討した選択肢と判断理由
- **Googleフォーム単体（画面遷移あり）**：無料・データ管理は楽だが、送信時にGoogle標準デザインの画面に遷移してしまい、LPの世界観が崩れる（心理的離脱要因になりうる）
- **Formspree等の外部フォームサービス**：LPの見た目を維持できるが、別アカウント登録・月間送信数制限あり
- **採用した方法：Googleフォーム＋隠しiframe送信**：無料・データはGoogleスプレッドシートに集計されつつ、LPの見た目とモーダル演出を一切変更せずに済む。ユーザー体験上の妥協が最小

### やったこと・再現手順
1. https://forms.google.com で新規フォームを作成し、質問を1つ（メールアドレス、記述式）追加
2. フォームの「送信」→リンクアイコンから viewform URL を取得
3. **entry IDの調べ方**：実際にHTMLを解析しなくても、以下のプレフィルURLをブラウザで開き、入力欄に値が反映されるかで検証できる（実データを送信せずに確認可能）
   ```
   https://docs.google.com/forms/d/e/{フォームID}/viewform?usp=pp_url&entry.{推測した番号}=verify-test-value
   ```
   ページのHTML内に埋め込まれた `FB_PUBLIC_LOAD_DATA_` という変数の中に、質問ごとの数値IDが入っている（`[[質問ID, タイトル, ..., [[entryID, ...]]]]`という構造）。ここから entry ID の候補を見つけ、上記URLで検証するのが確実
4. `index.html` に隠し`<iframe>`と、`entry.{ID}`を持つ隠し`<form target="送信先iframe" action="{フォームID}/formResponse">` を追加
5. 本物のフォーム送信ボタンのJSハンドラで、隠しフォームに値をセットして`.submit()`し、その後に今まで通りモーダルを表示

### 得られた成果物
- `index.html` 内の `#gform-proxy`（隠しフォーム）と `#hidden-submit-frame`（隠しiframe）
- Googleフォームの回答は、フォーム作成者のGoogleアカウントのスプレッドシートに自動集計される

### テスト方法（実データを汚さずに検証したい場合）
- 上記のプレフィルURL方式はGETリクエストなので、実際には送信されない（安全に何度でも試せる）
- 実際にPOSTで送信テストする場合（`.../formResponse`にPOST）は、本物の回答として記録されるので、テスト後にGoogleフォームの回答一覧からテスト行を削除すること

---

## 4. LPの公開（Netlify Drop）

### 目的
広告のリンク先として使える、実際に一般公開されたURLを用意する。

### やったこと・手順
1. https://app.netlify.com/drop を開く
2. `incomodo-lp-deploy.zip`（`index.html` + `privacy.html` + `assets/`一式）をドラッグ＆ドロップ
3. 匿名デプロイの場合、Netlifyの仕様で自動的にパスワード保護がかかる（パスワードはデプロイ直後の画面に表示される。今回は `My-Drop-Site`）
4. 「Claim your site」から無料アカウントを作成し、サイトを紐づける
5. サイト設定でアクセス範囲を **Public** に変更する（「Only team members can view」のままだとログインが必要になってしまう）
6. 更新時は、Netlifyのプロジェクト画面「Production deploys」欄にある「Drag and drop your project folder here」に、更新済みのZIPを再度ドラッグ＆ドロップすればよい（同じサイトに上書きデプロイされる）

### ハマった問題と原因（重要：ZIP作成時の注意）
- **問題**：デプロイ後、画像（`assets/`配下）だけが404になった
- **原因**：Windows PowerShellの `Compress-Archive` コマンドは、サブフォルダを含むZIPを作る際に、フォルダの区切り文字を `\`（バックスラッシュ）で記録してしまうことがある。ZIP規格上は `/`（スラッシュ）が正しく、Netlifyを含む多くのホスティングはこれに従って展開するため、`assets\shot-map.jpg` のような誤ったパスは正しく展開されない
- **対処法**：`Compress-Archive` を使わず、`System.IO.Compression.ZipArchive` を使い、エントリ名を明示的にスラッシュ区切りで指定してZIPを作る。再現コード：
  ```powershell
  Add-Type -AssemblyName System.IO.Compression
  $fs = [System.IO.File]::Open($zipPath, [System.IO.FileMode]::Create)
  $archive = New-Object System.IO.Compression.ZipArchive($fs, [System.IO.Compression.ZipArchiveMode]::Create)
  $entry = $archive.CreateEntry("assets/shot-map.jpg", [System.IO.Compression.CompressionLevel]::Optimal)
  $entryStream = $entry.Open()
  $bytes = [System.IO.File]::ReadAllBytes($localFilePath)
  $entryStream.Write($bytes, 0, $bytes.Length)
  $entryStream.Close()
  # ...他のファイルも同様に追加...
  $archive.Dispose(); $fs.Close()
  ```
- デプロイ後は必ず、`curl`やブラウザで画像URL（例：`{公開URL}/assets/shot-map.jpg`）に直接アクセスし、200が返るか確認すること
- **注意（重複サイトの発生）**：ZIPをNetlify Dropに再アップロードする際、既存サイト（snazzy-parfait-172aba）の「Production deploys」欄ではなく、トップページの新規ドロップ欄にドラッグしてしまうと、別の新しいサイト（例：`clever-gecko-06618d.netlify.app`）として作成されてしまう。ピクセル設置後の試行錯誤中にこれが発生し、同じピクセルコードを含む古いサイトが残ってイベント計測にノイズが入っていた（2026/09/03に発見・削除済み）。**再デプロイ時は、必ず既存サイトのProduction deploys欄にドロップすること**

### 得られた成果物
- 公開URL：https://snazzy-parfait-172aba.netlify.app
- `incomodo-lp-deploy.zip`（常に最新のindex.html/privacy.html/assetsを反映するよう再生成する）

---

## 5. Meta広告の出稿準備

### 5-1. Facebookページ／Instagramビジネスアカウント

#### 目的
Meta広告（Instagram含む）を出稿するには、Facebookページへの紐づけが前提条件になる。

#### やったこと
1. https://www.facebook.com/pages/create でFacebookページ「Incomodo」を作成
2. Instagramで新規アカウントを作成し、「設定→アカウントの種類とツール→プロアカウントに切り替える→ビジネス」から、作成したFacebookページと連携

#### 補足（判断理由）
- 広告出稿に**必須**なのはFacebookページのみ。Instagramアカウントは技術的には無くても広告は出せるが、広告をタップした人がプロフィールを見た際の信頼感のために作成した

### 5-2. Meta Business Manager／広告アカウント・支払い設定

#### 目的
実際に広告費を支払い、配信を行うための課金基盤を整える。

#### やったこと・手順
1. https://business.facebook.com でビジネスアカウント作成（ビジネス名：Incomodo）
2. 「ビジネス設定」→「アカウント」→「ページ」「Instagramアカウント」にそれぞれ追加
3. 「ビジネス設定」→「アカウント」→「広告アカウント」→新規作成
   - タイムゾーン：`(GMT+09:00) 大阪、札幌、東京`
   - 通貨：`日本円（JPY）`
   - **注意：タイムゾーンと通貨は後から変更できない**
4. 「広告マネージャ」→「支払い設定」からクレジットカードを登録

### 5-3. Metaピクセル（コンバージョン計測）の設置

#### 目的
広告経由でLPに来た人が実際に何人登録したかを正確に計測し、CPA（獲得単価）をもとにGo/NoGo判断をするため。これがないと「広告が効いているか」を定量的に判断できない。

#### やったこと・手順
1. https://business.facebook.com/events_manager にアクセス
2. 「データをリンク」→「ウェブ」→「Metaピクセルのみ」を選択
3. ピクセル名：`Incomodo Pixel`、URL：LPの公開URLを入力
4. 設置方法は「コードを自分で追加」を選択し、発行されたピクセルID（`1714208769855921`）を取得
5. `index.html` の `<head>` に、Metaピクセルの基本コード（PageViewを計測）を追加
6. フォーム送信の成功処理（Googleフォームへの隠しiframe送信の直後）に `fbq('track', 'Lead')` を追加し、「登録完了」をコンバージョンイベントとして計測できるようにした
7. `privacy.html` のCookie/解析ツールの章に、Metaピクセル導入の旨を明記
8. LPを再デプロイし、本番URLでピクセル発火・Leadイベントのコードが含まれていることを確認

#### 得られた成果物
- `index.html` に埋め込まれたMetaピクセルコード（ID: `1457252082902363`）とLeadイベント発火処理

#### ハマりどころ：ピクセルが2つできてしまった問題
- **症状**：Events Managerで作った最初のピクセル（`1714208769855921`）は、実際にイベントを受信していた（Events Managerの「データセット」概要画面で確認できた）にもかかわらず、広告キャンペーン作成画面の「データセット」プルダウンには一切出てこなかった
- **原因**：`https://business.facebook.com/events_manager` に直接アクセスして作成したピクセルは、**ビジネスポートフォリオ「incomodo.app」に属さない、別の場所（個人のアカウント寄りの場所）に作られてしまっていた**。広告キャンペーンは特定のビジネスポートフォリオ／広告アカウントに紐づく資産（ピクセル含む）しか選択できないため、選択肢に出てこなかった
- **確認方法**：`https://business.facebook.com/settings` →「データソース」→「データセットとピクセル」を開くと、**そのビジネスポートフォリオに実際に属しているピクセルだけ**が一覧表示される。ここに出てこないピクセルIDは、いくら他の画面でイベントが見えていても、そのビジネスの広告には使えないと判断してよい
- **教訓（AI・作業者共通）**：Metaピクセルを作る際は、Events Managerに直接アクセスするのではなく、**必ず対象のビジネスポートフォリオ／広告アカウントの画面から辿って「データをリンク」する**（本マニュアル5-3の手順1は、実際には広告アカウントの「データセット」選択画面内の「作成」ボタンから行うのがより確実）。作成後は、`business.facebook.com/settings` のデータセット一覧に出てくるかを必ず確認してから、そのIDをLPのコードに使うこと
- 結果として、LPのコードとデプロイZIPを3往復ほど作り直すことになった。同じ間違いを繰り返さないよう、ピクセルIDを変更する際は必ずこのビジネス設定画面で存在確認をしてからコードに反映する
- **解決確認済み（2026/08/31）**：イベントマネージャで「Incomodo Pixel（1457252082902363）」の「概要」タブを見ると、「PageView」5件・「リード」1件がいずれも「アクティブ」状態で記録されていることを確認。ビジネスポートフォリオ配下で正しく計測できている状態になった。ここから広告キャンペーンをゼロから作成する作業に着手

### 5-4. 広告セットの設定：コンバージョンの場所・パフォーマンス目標

#### 目的
「リード」目的のキャンペーンでは、広告セット作成時に「コンバージョンの場所」を選ぶ必要がある。どこでユーザーに登録してもらうかによって、離脱率・計測方法・LPの見せ方が変わるため、選択理由を残しておく。

#### コンバージョンの場所の種類と効果

| 選択肢 | 概要 | メリット | デメリット |
|---|---|---|---|
| **ウェブサイト** | 広告タップ後、自社LPに遷移し、LP上のフォームで登録してもらう | LPのデザイン・世界観（クラフト紙アナログ演出）をそのまま体験してもらえる。ブランド訴求を兼ねられる | 遷移が1ステップ増える分、離脱が起きやすく、一般的にCPA（登録単価）はインスタントフォームより高くなりやすい |
| **インスタントフォーム（旧リード獲得広告）** | Facebook/Instagramのアプリ内で完結するネイティブフォームが開き、その場で登録できる（LPへの遷移なし） | 読み込み待ちがなく完了率が高い。一般的にCPAが下がりやすい。Metaが推奨として提示してくる | LPの世界観・ストーリーテリングを見せられない。フォーム項目もMetaのテンプレートに準ずる |
| アプリ | アプリのインストール・アプリ内アクションを計測する場合に使う | - | 今回はアプリ未リリースのため対象外 |
| Messenger/Instagram DM | メッセージのやり取りを起点にリードを取る | 会話ベースの見込み客育成に向く | 運用の手間が増える。今回のスモークテストの目的（数値でGo/NoGo判断）には過剰 |
| 通話 | 電話発信をコンバージョンとする | BtoB・高単価商材向き | 個人開発の無料事前登録には不向き |

#### 選び方の判断基準
- **LPのデザイン・コピー自体が「不便さを楽しむ」というコンセプトを伝える重要な要素になっている**場合は「ウェブサイト」を選ぶ（コンセプトが刺さるかどうかも同時に検証したいため）
- **とにかく低CPAで「興味を持つ人がどれだけいるか」の母数だけを早く知りたい**場合は「インスタントフォーム」を選ぶ
- 本プロジェクトでは、LPそのものの世界観・ストーリーテリングが訴求の核であり、Go/NoGo判断にはCPAの数字だけでなく「LPを最後まで読んでもらえたか」という質的な手応えも重視したいため、**「ウェブサイト」を採用**

#### やったこと
1. 「コンバージョンの場所」で「ウェブサイト」を選択
2. 「パフォーマンス目標」で「リード数の最大化」を選択（「コンバージョン値の最大化」は、Lead イベントに金額を紐づけていないため選択不可・かつ無意味なので対象外）
3. 「データセット」で「Incomodo Pixel」（1457252082902363）を選択
4. 「コンバージョンイベント」で「リード」を選択

### 5-5. キャンペーンの作成・公開（実施記録）

#### 目的
5-1〜5-4で整えた土台（ページ・広告アカウント・ピクセル・コンバージョン設定の考え方）を使って、実際にキャンペーンを作成し公開する。

#### 実際に設定した内容（2026/09/03 公開）
| 項目 | 値 |
|---|---|
| キャンペーン目的 | リード |
| 予算戦略 | キャンペーンの予算（Advantageキャンペーン予算オン）／通算予算 ¥10,000 |
| キャンペーン入札戦略 | 最大数量 |
| 広告スケジュール | 2026/09/03〜2026/09/10（7日間） |
| コンバージョンの場所 | ウェブサイト |
| パフォーマンス目標 | リード数の最大化 |
| データセット／イベント | Incomodo Pixel（1457252082902363）／リード |
| 地域／年齢 | 日本／20〜35歳（Advantage+オーディエンスにより上限は自動拡張されうる） |
| 詳細ターゲット設定 | 本（Books）・文学（Literature）・文筆（Writing） |
| 配置 | Advantage+配置（自動） |
| クリエイティブ | `ads/ad-feed-square-1080x1080.jpg`（手動アップロード、シングル画像） |
| リンク先URL | https://snazzy-parfait-172aba.netlify.app |
| 発見リンク | 使用しない（サイトと無関係な商品リンクが候補に出るため、チェックを入れずスキップ） |

#### 手順上の注意点（AI・作業者共通）
- **予算のスケジュール指定は広告セット単位**：キャンペーンレベルの「予算」設定画面には開始日・終了日の入力欄がない。「広告スケジュール」で「スケジュールを設定して広告を配信」を選ぶと、実際の期間指定は広告セットの設定画面に出てくる
- **「発見リンク」機能に注意**：クリエイティブ設定時に「発見リンクを管理」という項目が出ることがあり、自社サイトと無関係な商品（他の広告主のものと思われる画像）が候補として表示されることがある。「広告掲載中に、関連する発見リンクを特定して追加する」のチェックは入れずにスキップしてよい
- **URLパラメーターは現状不要**：LP側にGoogleアナリティクス等の解析ツールを導入していないため、トラッキング設定の「URLパラメーター」欄は空欄のままで問題ない
- 年齢の上限が選択できないのは、Advantage+オーディエンスが標準になっているため（下限のみ指定し、Metaが自動で範囲を拡張する仕様）

#### 得られた成果物
- Meta広告キャンペーン（目的：リード、予算¥10,000、期間2026/09/03〜09/10）が公開済み
- 結果の確認・Go/NoGo判定は本マニュアル6章の基準に沿って、配信終了後（2026/09/10以降）に行う

---

## 5-6. 事前登録者への自動サンクスメール＋ヒアリング（Google Apps Script）

### 目的
- 登録直後にお礼メールを送ることで、登録者に安心感・信頼感を持ってもらう
- 単なるお礼で終わらせず、返信という形で定性的なユーザーの声（既存SNSへの不満点）を集め、Go/NoGo判断やLP・プロダクトの改善材料にする（数値だけのCPA判断を補強する狙い）

### やったこと
- Googleフォームに紐づくApps Scriptプロジェクトに、フォーム送信をトリガーとする`onFormSubmit(e)`関数を実装
- フォームの質問項目「メールアドレス」の回答を`e.response.getItemResponses()`から質問タイトルで検索して取得し、`MailApp.sendEmail()`で送信
- インストーラブルトリガー（フォームから／フォーム送信時）を設定し、送信のたびに自動実行されるようにした

### 判断の理由
- スプレッドシート側の列番号や順序に依存する実装（`e.namedValues['メールアドレス']`等）ではなく、フォーム自体のアイテム（質問）タイトルから検索する実装にした。理由：将来フォームに質問を追加・並び替えしても、質問文「メールアドレス」さえ変えなければ壊れないため
- メール本文に、開発の背景（個人開発・スローコミュニケーションというコンセプト）と、返信を促すヒアリング文言を含めることで、事前登録という受動的なアクションを、能動的なフィードバックのきっかけに変える設計にした
- 署名は「Incomodo開発者　後藤雅也」と実名を明記。プライバシーポリシーで既に運営者名を公開しているため、実名を出すデメリットはなく、個人開発者が直接返信を読んでいるという信頼感がヒアリングへの返信率を高めると判断した

### 得られた成果物
- フォーム紐づけのApps Scriptプロジェクト内の`onFormSubmit`関数
- インストーラブルトリガー（フォーム送信時に自動実行）

### 今後の運用時の注意点
- 返信メールの宛先は、送信元Gmailアカウント（フォーム作成者のアカウント）に届く。返信内容は都度目視で確認し、有用な意見はMANUAL.mdやprogress.htmlのバックログに反映する
- メール文面を変更したい場合は、Apps Scriptエディタの`body`変数を直接書き換えて保存するだけでよい（トリガーの再設定は不要）

### 過去registrant（トリガー設定前の登録者）への一括送信
- **目的**：`onFormSubmit`トリガーは設定後の新規登録にしか効かないため、それ以前に登録してくれた人には自動でメールが届かない。取りこぼしなく同じお礼＋ヒアリングメールを届けるため、一括送信用の関数を別途用意した
- **実装**：同じスクリプトファイルに`sendThankYouToPastResponses()`関数を追加。`form.getResponses()`で全回答を取得し、既に自動送信済み（トリガー設定後に登録され、`onFormSubmit`で個別に送信済み）のメールアドレスを`ALREADY_SENT`配列で除外してから、残りの回答者に同文面のメールを送信する
- **二重送信防止の考え方**：トリガー設定後に届いた新規登録分（テスト送信1件＋実登録2件）のメールアドレスを`ALREADY_SENT`にあらかじめ手動で入れておくことで、それらをスキップする設計にした
- **実行結果（2026/09/06）**：8件に一括送信完了
- **再実行する際の注意**：この関数は「送信済みリストで自動的に重複を防ぐ」仕組みではなく、`ALREADY_SENT`の手動リストに頼っている。将来また過去分をまとめて送りたくなった場合は、その時点までに自動送信済みのアドレスを都度`ALREADY_SENT`に追記してから実行すること

---

## 6. 需要検証の目標設定（予算・KPI・Go/NoGoの基準）

### 目的
「いくら使ったら、どんな結果ならどう判断するか」を事前に決めておくことで、広告配信後に感情的・場当たり的な判断をしないようにする。

### 決定内容
| 項目 | 内容 |
|---|---|
| テスト予算 | ¥10,000（副業として無理のない範囲） |
| 目標CPA | ¥150〜300 |
| 想定登録件数 | 予算÷CPAで 約33〜66件 |
| テスト期間 | 最低5〜7日間（Metaの配信最適化には数日かかるため） |
| **Go基準** | 30件以上の登録、かつCPAが¥300以内。または件数が少なくても明確な反応（保存・シェア・コメント等）がある場合 |
| **要見直し基準** | クリック（LP来訪）はあるが登録に至らない → LP・コピーの問題の可能性。文面やターゲティングを調整して再テスト |
| **No-Go基準** | CPAが¥500を大きく超える、またはクリック自体が少なく反応が薄い |

### 判断の理由
¥10,000という金額は統計的に厳密なCPAを出すには少ないため、**数字の精緻さより「手応えがあるかどうか」の肌感覚判断**という位置づけにしている。

---

## 7. 進捗管理の運用（progress.html）

### 目的
やるべきタスク・課題を可視化し、番号で参照しやすくすることで、指示のやり取りを簡潔にする。

### ルール
- ロードマップの各タスクには `P{フェーズ番号}-{通し番号}`（例：`P1-1`）を付与
- バックログの各課題には `B{通し番号}`（例：`B1`）を付与（現在 `B1`〜`B30`）
- 既存タスクを途中で分割する場合は、後続タスクの番号を振り直さず、`p3-2b` のようなidで追加する（バッジは`P3-2B`と表示される）。番号を振り直すと、過去の記録や指示の中の番号とずれてしまうため
- 開発中に新しく見つかった課題はその都度バックログに追加し、ロードマップのタスクが「途中まで完了」の場合は分割して見える化する
- 番号は、各項目のチェックボックスの`id`属性（`p1-1`, `i1`など）から自動生成される（`progress.html`内の`injectItemCodes()`関数）。新しい項目を追加する際は、既存の命名規則（`pフェーズ-連番` / `i連番`）に沿ったidを振れば自動的にバッジが付く
- チェック状態はブラウザの`localStorage`に保存される（サーバー保存ではないため、別ブラウザ・別端末では引き継がれない点に注意）

---

## 8. 広告クリエイティブの制作

### 目的
LP自体とは別に、Instagram/Facebookのフィード・ストーリーズに実際に表示される広告画像とコピーが必要。

### 判断の理由
- 画像生成AIに頼らず、LPと同じHTML/CSSの技術で作成した。理由：LPの既存デザイントークン（配色・フォント・postmarkモチーフ）とアプリ画面モックアップ画像（`assets/shot-*.jpg`）をそのまま流用でき、ブランドの一貫性が保てるため
- 制作方法：指定サイズ（例：1080×1080）ちょうどのHTMLファイルを作り、headless Chromeで`--window-size`をそのサイズに指定してスクリーンショットを撮ることで、正確なピクセルサイズの広告画像を書き出した

### やったこと
1. フィード用（正方形 1080×1080）：`shot-map.jpg`を主役に、見出し・CTAバッジを配置
2. ストーリーズ用（縦長 1080×1920）：`shot-letter.jpg`を主役に、同様のレイアウトを縦向けに再構成
3. 広告文面（プライマリテキスト／見出し／説明文）をLPの既存コピーのトーンに合わせて作成

### ハマりどころ
- 最初、スマホモックアップ画像を大きくしすぎて見出しテキストと重なった。**広告クリエイティブは要素同士が重ならないよう、画像サイズと文字の縦position を先に紙面全体でレイアウト計算してから実装するとよい**（1080×1080なら、上から：eyebrow 60-100px / 画像 120-770px / 見出し 790-950px / CTA 950-1040px、のように帯で区切って設計した）

### 得られた成果物
- `ads/ad-feed-square-1080x1080.jpg`
- `ads/ad-story-1080x1920.jpg`
- 広告コピー（プライマリテキスト・見出し・説明文・CTAボタン文言「今すぐ登録」）

---

## 9. 需要検証の結果とGo判定

### 目的
2026/09/03〜09/10に配信したMeta広告の実績を集計し、6章で定めた基準に沿って本格開発（Phase3）に進むか判定する。

### 実績
| 項目 | 値 |
|---|---|
| 消化金額 | ¥10,000 |
| 登録件数 | 37件 |
| CPA | ¥10,000 ÷ 37 ≈ ¥270 |

### 判定
6章のGo基準（**30件以上の登録、かつCPAが¥300以内**）を両方クリアしたため、**Go判定**（2026-09-10）。想定登録件数レンジ（33〜66件）にも収まっている。

### やったこと
- `progress.html` の P2-4（CPA計測・分析）・P2-5（Go/NoGo判定）にチェックを入れ、Phase 2を完了扱いに更新
- 統計タイル・バックログB7の記述に実績値を反映

### 得られた成果物
- 更新済みの `progress.html`（Phase 2完了、全体進捗50%）

---

## 10. 開発環境の方針（Windows中心・iOS対応方法）

### 目的
Phase3のMVP開発を始めるにあたり、開発機（Windows PC）とターゲットOS（iOS想定）のギャップをどう埋めるかを事前に決めておく。

### 前提となる制約
- 開発機はWindows。iOSアプリのビルドにはXcode（macOS専用）が必須で、Windows単体ではiOSシミュレータ実行・実機ビルド・App Store提出ができない
- 一方、Android開発（Flutter + Android Studio/エミュレータ）はWindows上で完結する

### 検討した選択肢
| 選択肢 | メリット | デメリット |
|---|---|---|
| Macを購入 | ローカルで両OSとも高速に開発・実機テストできる（ホットリロード可） | 初期投資が発生（Mac mini新品で10万円前後〜） |
| クラウドMacビルド（Codemagic等） | 初期投資ゼロ、Macを一切用意せずiOSビルド〜TestFlight配信まで可能 | push→ビルド待ち（10〜15分程度）が発生し、開発速度は劣る |

Codemagicの料金を試算した結果、節目ごとのビルド運用（週1回程度〜多くて毎日）であれば月間無料枠（500分/月、macOS M2）内に収まる見込みで、実質無料〜数ドル程度の負担で運用可能と判断した。

### 決定内容
- **日常の開発・デバッグはWindows上でAndroidを対象に行う**（Flutter + Android Studio、ホットリロードで高速反復）
- **iOS対応はCodemagicによるクラウドビルドで対応**し、機能がまとまった節目ごとにビルド → TestFlightで実機（iPhone）確認する運用とする
- Macの購入は見送り、開発量が増えて「クラウドビルドでは遅くて辛い」となった時点で改めて検討する
- Apple Developer Program（年$99）は、iOSでのTestFlight配信・提出が必要になるタイミング（Phase3後半〜Phase4）で登録すればよい。Google Play Console（$25）は先に必要になる

### 判断の理由
- 現時点ではMVPがまだ存在せず、頻繁なiOS実機デバッグが必要な段階ではない。まずAndroidで機能を作り切ることを優先し、iOSは定期的な健康診断（ビルドが通るか・TestFlightで動くか）で足りる
- 初期投資を抑えつつiOSリリースの道筋（Codemagic経由）は確保できるため、副業として無理のない範囲で進められる

### 補足：言語選定（Flutter/Dart vs React Native/TypeScript）
開発言語としてTypeScript（React Native/Expo）も比較検討したが、最終的に**Flutter/Dartを採用**（2026-09-10確定）。理由：実際のコード実装はAIが担当するため言語自体の馴染みやすさより、Incomodoの世界観（クラフト紙・凝った演出）をどこまで作り込めるかを優先した。FlutterはUIを自前描画するため、Android/iOSで見た目が完全に一致しやすい点を評価した。なおどちらの選択でもMacなしでのiOSクラウドビルド（Flutter→Codemagic／React Native→Expo EAS Build）は可能で、この観点での優劣はなかった。

---

## 11. 開発環境構築・Flutterプロジェクト作成（P3-1）

### 目的
Windows PC上でAndroid向けのFlutter開発ができる状態を整え、実際にビルドが通ることを確認する。

### やったこと・再現手順
1. Flutter SDK 3.47.3をダウンロードし、`C:\dev\flutter`に展開（OneDriveの同期対象外にするため、プロジェクトフォルダの外に配置）
2. Android Studioをインストール（`C:\Program Files\Android\Android Studio`）。インストーラーのサイレントインストール（`/S`オプション）は失敗したため、GUIウィザードを手動でクリックして進めた
3. Android SDK Command-line Toolsを別途ダウンロードし、`C:\dev\android-sdk`に配置（Android Studio本体には実SDKが同梱されていないため）
4. `sdkmanager`でライセンスに同意し、`platform-tools`・`platforms;android-36`・`build-tools;36.1.0`・`emulator`・`system-images;android-36;google_apis;x86_64`をインストール
5. `avdmanager`で仮想デバイス「Incomodo_Test」（Pixel 7・Android 16）を作成
6. 環境変数`ANDROID_HOME`・`ANDROID_SDK_ROOT`・`JAVA_HOME`（Android Studio同梱のJBR JDKを指定）をユーザー環境変数として永続設定し、`flutter\bin`・SDKの各toolディレクトリをPATHに追加
7. `flutter doctor`で全項目クリアを確認（Windowsデスクトップアプリ開発用のVisual Studio警告のみ残るが、Incomodoには不要なため無視）
8. リポジトリ内`app/`フォルダに `flutter create --org app.incomodo --project-name incomodo --platforms=android,ios app` でプロジェクトを新規作成
9. `flutter build apk --debug`で実際にビルドが通ることを検証（初回はGradle・NDK等の追加ダウンロードが発生し、ビルドに約8分かかった）

### ハマりどころ・注意点（コードを触るAI向け）
- **Android Studioインストーラーは無人インストール（`/S`）に対応していない**。GUIウィザードの手動クリックが必要
- **Android StudioにAndroid SDK本体は同梱されていない**。別途Command-line Toolsをダウンロードし、`sdkmanager`でコンポーネントを揃える必要がある
- **`sdkmanager --licenses`へのY入力は、PowerShellのパイプ（`"y" | sdkmanager`）だとうまく渡らないことがある**。`cmd /c "sdkmanager --licenses < yesfile.txt"`のように、ファイル経由でcmd.exeのリダイレクトを使うと確実
- **SDK・Flutter本体はOneDrive同期フォルダの外（`C:\dev`）に置く**。大量の小さいファイルで構成されるため、同期対象にすると動作が重くなる・同期エラーの原因になりうる。一方、アプリのソースコード自体（`app/`フォルダ）はリポジトリ内（OneDrive同期下）で問題ない

### 得られた成果物
- 動作確認済みのFlutter開発環境（Android向け）
- `app/`フォルダ内のFlutterプロジェクト雛形（パッケージID: `app.incomodo.incomodo`）
- ビルド済みの `app-debug.apk`（動作確認用、リポジトリには含めない）

---

## 12. Firebaseプロジェクト作成・Flutterアプリとの接続

### 目的
P3-2（ユーザー認証・ポスト登録機能）以降で使うFirebase（Authentication・Firestore・Storage）の土台を用意する。

### やったこと・再現手順
1. https://console.firebase.google.com で`incomodo.app@gmail.com`にログインし、プロジェクト「Incomodo」を作成（プロジェクトID: `incomodo-c161e`、Google Analytics連携は任意でどちらでもよい）
2. Firebase CLIをNode.js無しで導入するため、スタンドアロンバイナリ（`firebase-tools-win.exe`）をダウンロードし`C:\dev\firebase.exe`として配置
3. `firebase login`でGoogleアカウント認証。ブラウザが自動起動しない環境だったため、表示されたURLを手動で開き、認証コードを`firebase login <コード>`に渡す方式で完了
4. `dart pub global activate flutterfire_cli` でFlutterFire CLIを導入
5. `flutterfire configure --project=incomodo-c161e --platforms=android,ios --android-package-name=app.incomodo.incomodo --ios-bundle-id=app.incomodo.incomodo` を実行し、Firebase上にAndroid/iOSアプリを登録。`app/lib/firebase_options.dart`が自動生成される
6. `flutter pub add firebase_core` でパッケージを追加し、`app/lib/main.dart`の`main()`で`Firebase.initializeApp()`を呼ぶよう変更
7. `flutter build apk --debug` でFirebase連携込みのビルドが通ることを確認

### ハマりどころ・注意点（コードを触るAI向け）
- **`flutterfire`コマンドはWindowsでは`.bat`ファイル**。bash環境から素の`flutterfire`という名前では見つからないことがあるため、フルパス（`flutterfire.bat`）で呼ぶか、PATHが正しく通っているか確認する
- **FlutterFire CLIは内部で`firebase`コマンドをPATH経由で呼び出す**。`firebase.exe`を独自の場所（`C:\dev`）に置いた場合、そのフォルダをPATHに追加しておかないと「Found 0 Firebase projects」と誤判定され、新規プロジェクト作成の対話プロンプトが出て停止してしまう
- **`flutter build`が`Unable to delete directory`エラーで失敗する場合、犯人はOneDriveとは限らない**。今回は(a) `app/build`をOneDrive同期対象外にジャンクションで逃がす、(b) 前回ビルドの残骸のGradleデーモン（`java.exe`プロセス）を`taskkill`で終了する、の両方が必要だった。ビルド失敗時はまずこの2点を疑うとよい
- **`app/build`は物理的に`C:\dev\build-cache\incomodo-app-build`に存在し、`app/build`はそこへのNTFSジャンクション**。フォルダの中身を直接見たい場合はどちらのパスからでも同じ内容が見える

### 得られた成果物
- Firebaseプロジェクト「Incomodo」（`incomodo-c161e`）、Android/iOSアプリ登録済み
- `app/lib/firebase_options.dart`（自動生成、コミット対象）
- Firebase初期化コード込みでビルド確認済みの状態

---

## 13. アプリの設計方針とGoogleログインの実装（P3-2前半）

### 目的
ユーザー認証（ログイン）を実装する。あわせて、後からUIを差し替えやすいコード構成の土台を作る。

### 決定内容と理由
- **ログイン方式：Apple/Googleソーシャルログインの両対応**。App Storeの審査要件上、他社ソーシャルログインを提供する場合はSign in with Appleも原則必須のため。ただし実装順は**Google→Apple**。Apple Sign-InはXcode側の設定とApple Developer Program（年$99）登録が必要なため、iOS対応（Codemagic）に着手するタイミングで追加する
- **UIとロジックの分離（Riverpodで状態管理）**。PRDにUIデザイン要件が独立して存在し、P3-5で後からクラフト紙デザインを当てることが確定しているため。これにより見た目の刷新時にロジックへ触れずに済む。ただし新機能の追加コスト自体がゼロになるわけではない（壊さずに積み増せる、という効果）

### コード構成（`app/lib/`）
| フォルダ | 役割 | 変更頻度 |
|---|---|---|
| `models/` | 業務ルール（ポスト・手紙・切手・ID）。Firebaseに依存しないので端末なしでテストできる | ルールを変えるとき |
| `services/` | Firebase・Googleログインとのやり取りだけを担当 | ほぼ変わらない |
| `providers/` | Riverpodの状態定義（ログイン状態、ポスト、手紙、切手など） | ほぼ変わらない |
| `screens/` `widgets/` | 画面の見た目。色は直接書かず`theme/`から読む | 画面を変えるとき |
| `theme/` | 配色・フォント（`incomodo_theme.dart`、22章） | **見た目を差し替えるときはここ** |

（2026/09/16更新。この章を書いた時点では`models/`と`theme/`は空だった）

`main.dart`の`AuthGate`がログイン状態を監視し、未ログインならログイン画面、ログイン済みならホーム画面を出し分ける。

### やったこと・再現手順
1. `flutter pub add flutter_riverpod google_sign_in firebase_auth cloud_firestore firebase_storage`
2. デバッグ用署名鍵のSHA-1を取得し（`keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android`）、`firebase apps:android:sha:create`でFirebaseに登録
3. Firebaseコンソール → Authentication → Sign-in method で**Google**を有効化（公開名は「Incomodo」、サポートメールは`incomodo.app@gmail.com`、Web SDK構成は触らない）
4. **Google有効化の後で**`google-services.json`を再取得（`firebase apps:sdkconfig ANDROID <appId> -o ...`）
5. `auth_service.dart`で`GoogleSignIn.instance.initialize(serverClientId: <Web用クライアントID>)`→`authenticate()`→取得した`idToken`でFirebaseにサインイン
6. エミュレータ（Play Store入りイメージ）にGoogleアカウントを追加し、ログイン→「ようこそ、Incomodo公式さん」まで到達することを確認

### ハマりどころ・注意点（コードを触るAI向け）
- **`google_sign_in` v7以降はAPIが大きく変わった**。`GoogleSignIn.instance`（シングルトン）で`initialize()`→`authenticate()`の順。旧来の`GoogleSignIn().signIn()`は使えない。トークンは`account.authentication.idToken`で取得（accessTokenは含まれない）
- **Androidでは`serverClientId`の指定が必須**（未指定だと`clientConfigurationError: serverClientId must be provided on Android`）。値は`google-services.json`内の`client_type: 3`（Web用）のクライアントID。Androidなのに「Web用」のIDを使う点が紛らわしい
- **`google-services.json`は、Firebase側でGoogleログインを有効化した「後」に取得し直すこと**。有効化前に取得すると`oauth_client`が空で、Web用クライアントIDが入っていない
- **エミュレータは`google_apis_playstore`（Play Store入り）のシステムイメージが必要**。`google_apis`（Play Storeなし）では、新しいログイン方式（Credential Manager）に必要なモジュール`identity_credentials_platform`が無効化されており、ログインが「Checking info...」で止まる。仮想デバイスは「Incomodo_Play」を使う（旧「Incomodo_Test」はGoogleログイン不可）
- **エミュレータを一度終了すると、中断したセッションがスナップショットから復元され失敗しがち**。不調時は`emulator -avd Incomodo_Play -no-snapshot-load`でコールドブートする
- **このPC（メモリ8GB）では、エミュレータ起動直後に「System UI isn't responding」が出やすい**。「Wait」を押して1分ほど待てば落ち着く
- **エミュレータの操作・画面確認はAIがadb経由で行える**（`adb shell input tap X Y`、`adb exec-out screencap -p > file.png`で撮った画像をAIが読む）。人間の手作業が必要なのは、エミュレータへのGoogleアカウント追加（パスワード入力）の初回1回のみ

### 得られた成果物
- Googleログインが端から端まで動作するアプリ（ログイン→ホーム画面→ログアウト）
- UIとロジックを分離したコード構成の土台

### 残課題（リリース前に対応）
- **OAuth同意画面の整備**：ログイン時の同意画面でアプリ名が小文字の「incomodo」になっており、プライバシーポリシー・利用規約のリンクも未設定。Google Cloudコンソールの同意画面設定で、表記を「Incomodo」に直し`privacy.html`を紐付ける必要がある（利用規約はB15で作成予定）

---

## 14. ポスト登録機能（P3-2B）とFirestore

### 目的
要件定義書F-01の中核である「手紙の受け取り場所（ポスト）」を登録できるようにする。最大4箇所（自宅1＋拠点3）、追加・移設後48時間は工事中。

### 設計の判断と理由
- **Firestoreは東京リージョン（`asia-northeast1`）**。利用者が国内中心のため。**リージョンは後から変更できない**（2026-09-16、ユーザー確認のうえ決定）。誤削除防止のため削除保護も有効化（こちらは後から解除可能）
- **保存場所は`users/{uid}/posts/{枠ID}`、枠IDは`home`・`slot1`〜`slot3`の固定4つ**。件数を数えなくても「最大4箇所・自宅は1つ」がデータの形そのもので守られ、セキュリティルールでも強制できる（Firestoreのルールはコレクションの件数を数えられないため、この形にした）
- **工事期間の起点`constructionStartedAt`はサーバー時刻**。アプリから過去の時刻を送って工事をスキップする不正を、セキュリティルールで拒否できる。稼働時刻（起点＋48時間）は保存せず、毎回計算する
- **場所を動かしたら工事はやり直し、名前の変更だけならそのまま**。要件定義書の「変更後は工事中」を「位置の変更」と解釈した。名前の変更まで工事扱いにすると不便なだけで、不正防止の意味がないため
- **削除して再登録しても工事は最初から**（新規作成は必ずサーバー時刻が必要なので、自然にそうなる）
- **位置情報はGoogleの位置情報サービスを経由せず、端末のGPSを直接使う**（`AndroidSettings(forceLocationManager: true)`）。後述のハマりどころ参照。30秒で取得できなければ打ち切り、「屋外など電波の届く場所で再度お試しください」と表示する

### コード構成（13章に追加されたもの）
| ファイル | 役割 |
|---|---|
| `lib/models/post.dart` | ポストの型と業務ルール（4つの枠、48時間の計算、名前・座標の入力チェック）。Firebaseに依存しない純粋なロジック |
| `lib/services/post_service.dart` | Firestoreへの読み書き（登録・名前変更・移設・削除、一覧の監視） |
| `lib/services/location_service.dart` | 現在地の取得（許可の確認、タイムアウト） |
| `lib/providers/post_provider.dart` | ポスト一覧・1分ごとに進む時計（残り時間の表示更新用）の状態定義 |
| `lib/widgets/post_slot_card.dart` | ポスト1枠分のカード（**P3-5でクラフト紙デザインに差し替える主な対象**） |
| `lib/screens/home_screen.dart` | ホーム画面（4枠の一覧と、登録・名前変更・移設・削除の操作） |
| `test/post_test.dart` | 業務ルールの自動テスト（10件） |
| `firestore.rules` | セキュリティルール（`firebase deploy --only firestore:rules`で反映） |

### やったこと・再現手順
1. 業務ルール（`post.dart`）と自動テストを作成。`flutter test`で10件合格（48時間の境界、4枠・自宅1つ、名前20文字以内、座標の範囲）
2. **FirestoreのAPIを有効化**（Google Cloudコンソール、`incomodo.app@gmail.com`でのログインが必要なためユーザーが実施）
3. `firebase firestore:databases:create "(default)" --location asia-northeast1 --delete-protection ENABLED --project incomodo-c161e`
4. `firebase deploy --only firestore:rules --project incomodo-c161e`（`app/firebase.json`に`firestore.rules`の場所を記載済み）
5. `flutter pub add geolocator`、AndroidManifestに位置情報の権限（`ACCESS_FINE_LOCATION`・`ACCESS_COARSE_LOCATION`）、iOSのInfo.plistに位置情報の利用目的（`NSLocationWhenInUseUsageDescription`）を追加
6. エミュレータのGPSを東京駅に設定し、AIがadbで操作して確認：**登録（工事中48時間）→名前変更（残り時間は変わらず）→渋谷駅へ移設（工事がリセット）→アプリ再起動後もデータが残っている**。いずれも本番のセキュリティルールを通過。GPSが取れない状態では**30秒で打ち切り、案内メッセージが出る**ことも確認
7. **削除は実機で未確認**（エミュレータがメモリ不足で不安定になり、テストを打ち切ったため）。コードは`delete()`の1行、ルールも「本人なら削除可」のみで、リスクは低いと判断。次にエミュレータを使う機会に確認する

### ハマりどころ・注意点（コードを触るAI向け）
- **FirestoreはAPIを有効化しないと作成できない**（403エラー）。有効化はGoogle Cloudコンソールでのログインが必要。有効化の反映には1分ほどかかる
- **`firebase firestore:databases:create`は失敗しても終了コード0を返す**。成功したかは出力の「Successfully created」で判断すること
- **geolocatorの標準設定（Googleの位置情報サービス経由）は、Googleの「位置情報の精度向上」への同意を毎回要求し、断ると`The location service on the device is disabled`で必ず失敗する**。テスト環境だけでなく、同意を断った実際のユーザーもポストを登録できなくなる。`forceLocationManager: true`で端末のGPSを直接使うよう修正した
- **エミュレータのGPS設定は`adb emu geo fix <経度> <緯度>`（経度が先）**。アプリが位置を問い合わせている最中に届く必要があるため、操作中は数秒おきに送り続けるとよい
- **adbでの文字入力（`adb shell input text`）は英数字のみ**。日本語の名前はテストで入力できない
- **このPC（メモリ8GB）でエミュレータのテストをするときは、ビルド後に残るGradleのJavaプロセスを必ず終了する**（`taskkill /F /IM java.exe`）。ビルド後も約760MBを抱えたまま残り、空きメモリが654MBまで減った結果、**アプリの画面が固まる→エミュレータのGPSが止まる→Androidが再起動する**という順で不安定になった
- **Gradleのメモリ上限はテンプレートの初期値（`-Xmx8G`）のままだと、8GBのPCではメモリを使い果たす**。`app/android/gradle.properties`で`-Xmx3G`に下げ、Windowsでビルドフォルダのファイルが削除できなくなる原因だったファイル監視も`org.gradle.vfs.watch=false`で無効化した。以後はロックによるビルド失敗が起きず、ビルド時間も約80〜150秒に短縮された

### 得られた成果物
- ポストの登録・名前変更・移設・削除ができるホーム画面
- 業務ルールの自動テスト10件
- 東京リージョンのFirestoreと、不正防止を含むセキュリティルール

### 残課題
- **セキュリティルールの「不正な書き込みを拒否できるか」の自動テストが未整備**。正しい操作が通ることはアプリで確認済みだが、過去の時刻を送る・5つ目の枠を作る・他人のポストを書き換える、といった不正が拒否されることは未検証。Firebaseのエミュレータを使ったテストが必要（公開前に対応）
- P3-3（50m以内での開封判定）では、**工事中かどうかの判定もサーバー側（セキュリティルールの`request.time`）で行う**こと。アプリ側の時計は端末の時刻に依存し、書き換えられるため

---

## 15. GPS連動の開封判定（P3-3）

### 目的
要件定義書F-01の「登録されたポストから半径50m以内に入った場合のみ、手紙を開封・閲覧できる」を実装する。

### 実装内容
- `post.dart`に`unlockRadiusMeters = 50`と距離計算（ハバーサイン公式）を追加。`canOpenLetters()`は**「工事が終わっている」かつ「50m以内にいる」**の両方を満たすときだけtrueを返す
- ホーム画面に**「ポストに着いたか確認」ボタン**を追加。押したときだけ現在地を取得し、各ポストまでの距離と、開封できるかどうかを表示する
- 表示は3通り：「ポストに到着（ここで手紙を開けます）」／「ポストに到着（工事が終わるまで開けません）」／「現在地から約○m」

### 判断の理由
- **現在地の取得は、ボタンを押したときだけ**にした。常時追跡はバッテリーを消耗するうえ、「行かなきゃ、読めない」という体験に対して、常に見張られている感覚は合わないため。ユーザーが「着いた」と思ったときに自分で確認する形にした
- 距離計算は`geolocator`の関数ではなく**自前のハバーサイン公式**にした。`models/`をFirebaseや端末機能に依存させない方針のため（テストが端末なしで動く）

### テスト
`flutter test`で全22件（うちP3-3分は12件）。エミュレータのGPSが不安定なため、**画面表示の検証はウィジェットテストで行った**（端末不要・数秒で完了）。到着時／到着したが工事中／遠方（7.0km）／境界の51m、の各表示を検証している。

### 重要な制約（今後の実装者・AI向け）
- **50m判定はアプリ側でしか行えない**。Firestoreのセキュリティルールは位置情報を検証できないため、「50m以内にいる」という条件をサーバー側で強制することは現状できない。工事期間（48時間）はサーバー時刻で強制できているが、距離は強制できない
- MVPではこれを許容する。位置を偽装してまで手紙を早く読んでも、損なわれるのは本人の体験だけで、他人に実害がないため
- 将来的に厳密にするなら、Cloud Functions（Blazeプラン必須）で開封リクエストをサーバー側で検証する形になる

---

## 16. プロフィールとIDでつながる機能（P3-3B 段階1）

### 目的
手紙は「誰に」送るものなので、**手紙機能（P3-3C）より先に、相手を特定できる仕組み**が必要だった。要件定義書F-02の宛先指定にあたる部分。

### 実装内容
- `models/user_profile.dart`：表示名（20文字以内）とID（英小文字・数字・`_`で3〜15文字）。`normalizeHandle()`が`@`除去・前後空白除去・小文字化をまとめて行うので、「@Goto 」と「goto」が同じIDとして扱われる
- `services/user_service.dart`：プロフィール作成、IDでの検索、友だちリクエストの送信・承認・拒否
- `screens/profile_setup_screen.dart`：初回ログイン直後に一度だけ表示。`main.dart`の`AuthGate`が「未ログイン → ログイン画面」「プロフィール未作成 → この画面」「それ以外 → ホーム」と振り分ける
- `screens/friends_screen.dart`：自分のID表示、IDでの検索、届いたリクエストへの応答、友だち一覧

### 判断の理由
- **IDは一度決めたら変更できない**仕様にした（表示名は変更可）。IDを変更できると、相手の手元にある「@goto」というメモが別人を指すようになってしまい、手紙の宛先としては危険なため
- **ID予約を`handles/{id}`という別コレクションで管理**した。Firestoreはドキュメントの作成を「既に存在するなら失敗」にできるので、同じIDを2人が同時に取ろうとしても片方しか成功しない（`WriteBatch`でプロフィール作成と同時にコミット）
- **「友だち」は一方向のリスト**にした。`users/{相手}/friends/{自分}`に自分が載っていることが、「その相手に手紙を出してよい」という許可そのものになる（ルールで強制）。承認すると同時に相手にもリクエストを送り返すので、結果的に双方向になる

### ハマりどころ・注意点
- ID重複時、`handles/{id}`の作成がルールで弾かれてFirebaseは`permission-denied`を返す。「権限がありません」ではユーザーに意味が通じないため、`HandleTakenException`に変換して「そのIDは既に使われています」と表示している

### テスト
`flutter test`で全34件。IDの正規化・バリデーションをテストで固定した。

---

## 17. 招待リンクとQRコード（P3-3B 段階2）

### 目的
IDを口頭や手入力で伝えるのは間違いやすい。**目の前の相手にはQRコード、離れている相手には招待リンク**でつながれるようにする。

### 実装内容
- `models/invite.dart`：招待リンクの生成（`linkFor`）と、貼り付けられた文字列からIDを読み取る処理（`handleFrom`）。リンク・`@id`・素のIDのどれでも受け付ける
- `widgets/invite_card.dart`：自分のQRコードとID、「招待リンクをコピー」ボタン。QR描画は`qr_flutter`パッケージ（Dartだけで描画するのでウィジェットテストで検証できる）
- 友だち画面の検索欄が**招待リンクの貼り付けにも対応**（`相手のID / 招待リンク`）
- LP（`index.html`）：`?id=xxx`付きで開かれたとき、ページ上部に「招待が届いています @xxx」というカードを表示し、IDをコピーできるようにした

### 判断の理由
- 招待リンクの宛先を**LPにした**。独自ドメインもアプリの公開もまだなので、アプリを持っていない人がリンクを開いても「何も起きない」状態を避けたかった。LPに着地すれば、相手のIDを見せたうえで事前登録に誘導できる
- リンクの形式は`.../?id=goto`。`Invite.site`定数を差し替えるだけで独自ドメインに移行でき、**既に配ったリンクも`handleFrom`が読めるまま**になるよう、パス形式（`/i/goto`）も読み取り対象にしてある
- **QRの読み取り（カメラ）は実装していない**。エミュレータでは検証できず、カメラ権限も増えるため。スマホ標準のカメラアプリでQRを読めばLPが開き、そこでIDを確認できるので、MVPとしては成立している（B24として記録）
- LP側のIDは**必ずテキストとして挿入**し、`^[a-z0-9_]{3,15}$`に一致しない限り表示しない。URLパラメータをそのままHTMLに入れるとスクリプト埋め込みの穴になるため

### テスト
`flutter test`で全46件（うち今回分12件）。リンク生成、リンク／`@id`／素のIDからの読み取り、余計なクエリが付いたリンク、IDのないリンク、QRカードの表示とコピー操作を検証。LPは`?id=goto`付き／なしの両方をローカルサーバで表示確認した。

---

## 18. テキストの手紙の送受信（P3-3C）

### 目的
要件定義書F-02（非同期・通知なしの手紙体験、配送完了／受取完了ステータス）とF-03のテキスト部分を実装する。これでアプリの中心となる体験が一周する。

### 実装内容
- `models/letter.dart`：手紙の封筒（差出人・送信日時・開封日時）と`statusLabel`、本文のバリデーション（1000文字以内）。`openablePost()`は「いま立っているポストのうち、工事が終わっているもの」を返す
- `services/letter_service.dart`：送信・受信一覧・送信一覧・開封・本文の取得
- `screens/letters_screen.dart`（届いた手紙／送った手紙のタブ）、`screens/letter_screen.dart`（1通の表示・開封）、`screens/write_letter_screen.dart`（宛先選択＋本文）
- `widgets/letter_card.dart`：一覧の1行。未開封の手紙には「「自宅」に着いています。ここで開けます」「ポストの近くではありません」「現在地を確認すると…」のいずれかを出す
- ホーム画面のAppBarに手紙アイコンを追加。**未開封の件数だけをバッジで出す**（通知は一切行わない）
- 「現在地」を`hereProvider`に移し、ホーム画面と手紙画面で同じ確認結果を使うようにした

### データの持ち方（3つのドキュメントを1回のバッチで書く）
| パス | 中身 | 意味 |
|---|---|---|
| `users/{受取人}/letters/{id}` | 差出人・送信日時・開封日時 | 封筒。受取人だけが読める |
| `users/{受取人}/letters/{id}/content/body` | 本文 | **開封済みになるまでサーバーが渡さない** |
| `users/{差出人}/sentLetters/{id}` | 宛先・本文・開封日時 | 差出人の控え。開封されると受取人がここも更新する |

### 判断の理由
- **宛先は「人」であって「ポスト」ではない**。要件定義書に「送り手は相手のポスト位置を知る必要はなく」とあるとおりで、差出人が相手のポストを選べる作りにすると、**相手の家の場所が差出人に分かってしまう**。受取人は自分のどのポストでも開けられる
- **本文を封筒と別のドキュメントに分けた**。同じドキュメントに入れると、未開封の手紙の本文もスマホにダウンロードされてしまい、「行かなきゃ、読めない」という前提がキャッシュを覗くだけで崩れる。分けたうえで、ルール側で`openedAt != null`のときしか読めないようにしてある
- 送信は**バッチ書き込み**。3つのうち1つでも失敗したら全部取り消されるので、「封筒はあるのに本文がない手紙」が生まれない
- ルールでは`getAfter()`を使い、「本文は、同じバッチで作られた封筒の差出人本人しか書けない」ことを検証している

### 残っている制約
- **「50m以内にいるか」の判定は、いまもアプリ側でしかできない**（B23）。ただし本文が開封後にしか降りてこなくなったので、位置を偽装せずに読む、という抜け道は塞がった
- ~~手紙の削除・整理機能はまだない（B25）~~ → 27章で実装（本文も一緒に消している）

### テスト
`flutter test`で全63件（うち今回分17件）。ステータスの表示（配送完了→受取完了／未開封→開封済み）、本文のバリデーション、`openablePost()`（工事中は対象外・遠いポストは対象外・複数あるとき到着済みのものを選ぶ）、一覧カードの3通りの案内文、日付表示を検証した。ルールは`firebase deploy --only firestore:rules`で反映済み（コンパイル成功）。

---

## 19. セキュリティルールの自動検証（B22の解消）

### 目的
「正しい操作が通ること」はアプリを動かせば分かるが、**「不正が拒否されること」は動かしても分からない**。ルールの穴は、試さない限り見つからない。

### なぜエミュレータでの手動テストではなく、この方法にしたか
手紙の送受信は2人いないと試せない。エミュレータに2つ目のGoogleアカウントを追加するにはパスワード入力が要るため、**作業者が不在の間に自動で通しテストを行うことはできない**。そこで、Firestoreエミュレータに対して**2人のユーザーを名乗って直接リクエストを送る**方式にした。

### 実装内容
- `app/tool/rules_check.dart`：FirestoreエミュレータのREST APIを直接叩く検証スクリプト。**Node.jsは不要**（Dart SDKとエミュレータだけで動く）
- エミュレータは署名を検証しないので、ユーザーIDを書いた未署名のJWTを作れば、その人になりすませる。`Bearer owner`は管理者扱いでルールを無視するので、前提データの用意に使う
- `app/firebase.json`にエミュレータ設定（ポート8080）を追加

### 実行手順
```
cd app
firebase emulators:start --only firestore --project incomodo-c161e
dart run tool/rules_check.dart
```

### 検証している内容（39件）
| 分類 | 例 |
|---|---|
| 手紙の配送 | 友だちでない人は送れない／他人の名前を騙れない／過去の日時で送れない／封筒なしで本文だけ置けない |
| 開封 | 未開封の本文はサーバーが渡さない／差出人は封筒を読めない・開封できない／開封後だけ本文が読める／本文は書き換えられない |
| 切手 | 勝手に増やせない／同じ日に2回補充できない／明日の日付にできない／上限を超えない／他人の財布を読めない |
| 切手と手紙 | 切手を使わずには送れない／1通で2枚減らせない／切手切れなら手紙も送られない |
| ポスト（B22） | 工事開始を過去にできない／5つ目を作れない／他人のポストを読み書きできない／移設して工事期間を据え置けない |
| プロフィール | 他人のIDを予約できない／IDを後から変えられない／他人の友だちリストに自分を入れられない |

### 判断の理由
- エミュレータのGUI操作は、このPCのメモリ（8GB）では安定しない（B21）。**画面の配線は`test/letters_flow_test.dart`（画面を偽のサービスに繋いで動かすウィジェットテスト）で、データとルールは`rules_check.dart`で**、と役割を分けた。どちらも端末不要・数秒で終わる
- `letters_flow_test.dart`では、テスト側でも`AuthGate`と同じように認証とプロフィールを`watch`している。画面は`ref.read`で読むだけなので、誰も購読していないと値が来ないため

---

## 20. 切手システム（P3-6）

### 目的
要件定義書F-05のうち、**無料切手の配布と消費**を実装する。有料販売（B26）はリリース後。

### 実装内容
- `models/stamp_wallet.dart`：1日 `dailyRefill`＝2枚、上限 `maxHeld`＝10枚。`jstDate()`が「日本の今日」を返す
- `services/stamp_service.dart`：`users/{uid}/stamps/wallet`（残枚数と最終補充日）。`ensureToday()`はホーム画面を開いたときに呼ばれ、その日まだ配っていなければ配る
- 消費は**手紙の送信バッチの中**で行う。`letter_service.dart`が`FieldValue.increment(-1)`を同じバッチに含める
- 手紙を書く画面に残枚数を表示。0枚なら送信ボタンが押せない

### 判断の理由
- **無料切手はマネタイズではなく体験の制約**。要件定義の「送信数に自然な制約をかける」がそれで、1日に何十通も送れると「通知に追われるSNS」に近づく。だからPhase 3（MVP）に入れた
- **貯めこめない**（上限10枚）。1か月放置しても1か月分の手紙を一晩で書けるようにはしない。逆に、**留守にしていた日数分はさかのぼって配らない**。切手はアプリを開いた日のリズムを刻むもので、残高ではないと考えた
- **ルール側で「切手なしでは送れない」を強制した**。`getAfter()`で、同じバッチの中で差出人の残枚数がちょうど1枚減っていることを検証している。これがないと、アプリを改造すれば切手を払わずに送れてしまう

### 重要な制約
- **「今日」は日本時間で判定している**（ルールの`today()`と`jstDate()`が対）。日本以外で使うときは、この2つを同時に直さないと、切手が届く時刻がその人にとって中途半端な時間になる

### テスト
`flutter test`で全82件（うち今回分9件）。日付の境界（UTCの15:30は日本ではもう翌日）、上限、留守にしていた場合、残枚数の表示、0枚のときに送信できないこと。ルール側は`rules_check.dart`で10件（19章参照）。

---

## 21. 実機（エミュレータ）での2アカウント通し確認（B27）

### 目的
自動テストは「書いたとおりに動くこと」しか確かめられない。**2人の人間が実際に使うと何が起きるか**は、動かしてみないと分からない。

### やったこと
エミュレータに2つ目のGoogleアカウント（`masayanoyabou@gmail.com`）を追加してもらい、`incomodo.app@gmail.com`との間で以下を通しで確認した。アカウントの追加はパスワード入力が必要なため作業者にお願いし、それ以外の操作はすべてadb経由で自動で行った。

| 確認したこと | 結果 |
|---|---|
| プロフィール作成（@incomodo / @masaya） | OK |
| 自分のQRコードと招待リンクの表示 | OK |
| **招待リンクを貼り付けて相手を検索** | OK（`?id=`からIDを読み取れた） |
| 友だちリクエストの送信・受信・承認 | OK |
| 切手の初回配布（2枚） | OK |
| 手紙の送信 | OK（「配送完了」） |
| **切手が2枚→1枚に減る** | OK |
| 受信側の未開封バッジ | OK |
| 未開封の手紙の本文が表示されないこと | OK |
| 現在地の取得と距離表示（約6.5km） | OK |
| **手紙の開封** | **未確認**（下記） |

### 開封だけ確認できていない理由
受け取り側のポスト（TokyoHome）が**工事中（48時間）だった**ため。アプリ自身のルールが待たせている状態で、これ自体は正しい動作。工事が明ける頃に、GPSをポストの座標に合わせて開封→送信側が「受取完了」になることを確認する。なお、開封の可否・本文の開示はルール側で`rules_check.dart`により検証済み（19章）。

### 見つかった問題（2件とも修正済み）
1. **相手が承認しても、自分が折り返しのリクエストを承認するまで手紙を書けない**のに、「手紙を書く」画面は「まだ友だちがいません」としか言わず、承認待ちのリクエストがあることに気づけなかった。→ 承認待ちがあるときはその件数とリンクを出し、ホーム画面の友だちアイコンにもバッジを付けた
2. **工事中のポストの前に立っているとき、「ポストの近くではありません」と表示された**。実際には着いているので、ユーザーから見ると壊れているように見える。→ `postYouAreAt()`を追加して「着いているが工事中」を区別し、「「自宅」に着いていますが、まだ工事中です。あと○時間○分に開けられるようになります。」と出すようにした

### ハマりどころ・注意点
- **エミュレータを再起動するとスナップショットが復元され、古いビルドのアプリに戻ることがある**。`-no-snapshot-load`を付けて起動すること。一度これに気づかず、AppBarにアイコンが無いことで発覚した
- **ルールを先にデプロイし、アプリを古いビルドのまま動かすと送信が失敗する**（切手を使わない送信がルールで拒否されるため）。ルールを変えたら必ずAPKも作り直す
- このPC（メモリ8GB）では、エミュレータ実行中の空きメモリが0.4GB程度まで落ち、「System UI isn't responding」が頻発した（B21）。ビルドとエミュレータは**同時に動かさない**こと（ビルド→エミュレータ起動、の順にする）

---

## 22. クラフト紙風デザインの適用（P3-5）

### 目的
要件定義書§3（クラフト紙、素朴な温もり、明朝体）を反映し、**広告で事前登録してくれた人が見たLPと、アプリが同じものに見える**ようにする。

### 実装内容
- `lib/theme/incomodo_theme.dart`：LP（`index.html`）の配色をそのまま定義した`IncomodoColors`と、`buildIncomodoTheme()`。**画面側では色を直接書かず、必ずテーマから読む**（以前決めた「UIを後から差し替えやすく」の方針。見た目を変えるならこの1ファイル）
- `lib/widgets/postmark.dart`：LPの手紙モックと同じ意匠の消印（点線の外円、実線の内円、INCOMODO POST、日付）。ログイン画面と、開封した手紙に押される
- `lib/widgets/letter_paper.dart`：点線の余白と、文字の行に合わせた罫線のある便箋。短い手紙でも便箋に見えるよう最低の高さを持たせている
- ポストの状態色：稼働中は苔色（moss）、工事中はアンティークゴールド。信号機のような緑・橙はやめた

### 判断の理由
- **明朝体は端末内蔵のものを使い、フォントを同梱しない**。iOSはヒラギノ明朝、AndroidはNoto Serif CJK。Noto Serif JPを同梱すると1ウェイトで約5MB増えるため
- **LPの色をそのまま使わず、小さい文字の色だけ濃くした**。LPの補助テキスト色（墨58%）やゴールドは、スマホの小さい文字では紙の上で読みにくい（コントラスト比4.5未満）。`test/theme_test.dart`で、文字に使う全色が紙・カードの上で4.5:1以上あることを自動で検証している。**色を調整してこのテストが落ちたら、テストを緩めずに色を濃くすること**
- **角丸をほぼなくした**（2px）。LPのカードは角が立っており、Material標準の丸いボタンは手紙の世界観に合わないため
- **ダークモードには対応していない**。紙の質感そのものがデザインの核なので、MVPでは明るいテーマに固定した

### ハマりどころ・注意点
- **消印の文字が内側の円からはみ出した**。AndroidのmonospaceフォントはLPのSpecial Eliteより横に広い。内側の円（半径27/80）はラベルの位置で幅が約46しかないので、文字サイズはSVGより小さくしてある。文字を変えるときは実機で円に収まるか確認すること
- iOSでの見た目（ヒラギノ明朝）は**Macがないため未確認**。Codemagicでビルドする段階で確認する

### 同時に見つけて直した不具合：友だちリクエストの無限往復
新しく付けた「友だちアイコンのバッジ」に、**すでに友だちの相手からのリクエスト**が表示された。原因は`acceptFriendRequest()`が**承認するたびに無条件で折り返しのリクエストを送っていた**こと。AがBに申請→Bが承認してAに折り返し→Aが承認すると、またBに折り返しが飛ぶ、が永遠に続く。
- 折り返しのリクエストに`reply: true`を付け、それを承認したときは折り返さないようにした
- すでに友だちの相手からのリクエストは画面に出さない（`unansweredRequests()`）。既に残っている不要なリクエストもこれで見えなくなる
- あわせて、リクエストのドキュメントに項目の制限がなかったので、ルールで形を検証するようにした（`rules_check.dart`は45件に）

### テスト
`flutter test`で全109件。見た目はエミュレータで、ホーム・手紙一覧・ログイン（消印）・便箋を確認した。開封済みの手紙に消印が押された状態は、まだ開封済みの手紙がないため実機では未確認（ウィジェットテストでは確認済み）。

---

## 23. 切手のデザインとマネタイズの方針（2026/09/16決定）

### 目的
要件定義書F-05の「特別デザイン切手を有料で販売」を具体化する。作業者から「手紙に切手を貼る感覚にしたい」「特別な切手に思いを込められることを価値にしたい」「特別デザイン・ガチャ・コラボでのマネタイズ」という案が出たため、その評価と進め方を決めた。

### 決めたこと
**1. 切手は手紙に「貼る」。未開封の封筒には、中身の代わりに切手だけが見える**
届いた手紙はポストに行くまで読めない。その間に見えるのが相手の選んだ切手になる。桜の切手なら「春の話かな」、特別な切手なら「何か大事なことかも」と想像できるので、**読めない時間そのものが楽しみになる**。「行かなきゃ、読めない」を最も強くする要素として位置づける。相手を思って切手を「選ぶ」行為自体も手紙の一部になる。

**2. マネタイズ案の評価**

| 案 | 採否 | 理由 |
|---|---|---|
| 特別デザインの切手 | 採用 | 見た目だけの違いで、買った人が有利にならない。「思いを込めて選ぶ」と一致する |
| 追加切手（枚数）の販売 | 採用 | 要件定義F-05のとおり。下記「追加切手について」 |
| 季節・記念の限定切手 | 採用 | 本物の記念切手と同じ感覚。希少性を「運」ではなく「時期」で出せる |
| コラボ切手 | 採用・**最優先** | 下記 |
| ガチャ | **採用しない** | 下記 |

**3. コラボは「その場所に行かないと手に入らない切手」にする**
郵便局の風景印やご当地スタンプと同じ発想。
- カフェ・書店・旅館：来店した人だけがもらえる切手。お店にとって集客になるので、**お店から費用をいただける**
- 自治体・観光地：町の切手。観光促進の予算と相性がよい
- 作家・イラストレーター：デザイン切手を作り、売上を分け合う

有料切手の売上はユーザー数に比例するが、**お店や自治体からの収入はユーザーが少ない初期でも成り立つ**。また、お店との直接契約にはAppleの手数料がかからない。これらの理由から、初期の収益源として最も有力と判断した。

**4. ガチャを採用しない理由**
- **コンセプトと正反対になる**。事前登録者は「通知に追われるのは、もうやめにしませんか？」に共感して集まった。「もう1回引きたくなる」ことで売上を作る仕組みは、そこから離れたい人に向けたアプリとして矛盾し、ブランドへの信頼を損なう
- **「選ぶ」価値が薄れる**。相手のために選んだ切手には思いがこもるが、偶然出た切手では同じ重みになりにくい
- **規制と手間**。コンプガチャは景品表示法で禁止。通常のガチャでもApp Store・Google Playは排出確率の表示を義務付けている

ランダム性の楽しさは、**お金を絡めない形でだけ**取り入れる。例：毎日届く無料切手の絵柄が日替わりで変わる、ランダムポスト（F-04）で珍しい切手が手に入ることがある。

**5. 追加切手について**
当初AI側は「枚数を買えると送信数の制約が崩れる」と懸念したが、作業者の判断で採用とした。理由：**受け取る側は通知も来ず、ポストに行くまで読めないので、何通届いても追い立てられない**。1日の枚数は主に送る側のペースを作るもので、「もっと書きたい人が買い足す」のは自然な課金になる。

決定事項（2026/09/16、作業者が決定）：
- **追加切手は特別デザインの切手としてセットで売る**（例：季節の絵柄の切手5枚）。枚数だけでなく「選んで貼る」楽しさも付き、買う理由が強くなる
- **買った切手は上限（10枚、20章）に含めない**。上限は無料配布を貯め込みすぎないためのもの。実装では、無料配布分と購入分を別々に数え、上限は無料配布分にだけかける
- **購入による切手の追加は、サーバー側（Cloud Functions）で購入記録を検証してから行う**。現在のルールは「1日1回2枚の補充」と「送信時に1枚減る」しか許していないので、アプリから直接増やす形にはしない（改造アプリで無料で増やせてしまう）。Blazeプランが必要

### 進め方（3段階）
| 段階 | 内容 | 管理番号 |
|---|---|---|
| 1. いま | 切手に絵柄を持たせる土台。手紙にどの切手を貼ったか記録し、未開封の封筒に表示。無料の切手を数種類用意して選べるようにする。**デザインは仮で、後から詰める** | P3-7 |
| 2. リリース前後 | 季節限定・場所限定の切手の仕組み | B29 |
| 3. 収益化 | 有料の特別切手（アプリ内課金）と、コラボの営業 | B26・B30 |

### 注意点（将来の実装者・AI向け）
- iOSでデジタルの切手を売るには**Appleの課金システムが必須**で、売上の**15〜30%が手数料**になる
- 有料販売の前に、**景品表示法**と**資金決済法（前払式支払手段）**の扱いを専門家に確認すること。特に「有料の切手を消費して手紙を送る」形が前払式支払手段にあたるかは判断が分かれうる
- 切手の**枚数**（1日2枚・上限10枚、20章）と**絵柄**は、データ上は別の概念として扱う。枚数は送信の可否、絵柄は表現。追加切手は両方を一緒に売るが、内部では「どの絵柄を何枚持っているか」として持つ
- コラボのデザインは、著作権・使用許諾の契約を事前に結ぶこと

---

## 24. 切手の絵柄を選んで貼る（P3-7）

### 目的
23章で決めた段階1。切手に絵柄を持たせ、手紙に貼り、**まだ開けていない手紙では切手だけが見える**ようにする。有料の絵柄や限定切手（B26・B29）を後から足せる土台にする。

### 実装内容
- `models/stamp_design.dart`：絵柄の**一覧だけ**（id・名前）。無料の絵柄は5種類（通常切手・桜・青空・夜・木の葉）。Flutterに依存しない
- `theme/stamp_art.dart`：各絵柄の**見た目**（地の色・インクの色・モチーフ）。**仮のデザイン**で、本物のイラストに差し替えるときはこのファイルだけ直す
- `widgets/stamp_view.dart`：ミシン目のある切手（`StampView`）と、横にスクロールして選ぶ`StampPicker`
- 手紙に`stampId`を記録（封筒と、差出人の控えの両方）。手紙を書く画面で選び、一覧・未開封の封筒・開封後の便箋（消印を重ねて押す）に表示

### 判断の理由
- **一覧（models）と見た目（theme）を分けた**。「デザインは後から詰める」方針なので、絵柄を差し替えてもロジックやルールに触れずに済むようにした
- **ルールで「無料の絵柄しか貼れない」ようにした**（`freeStamp()`）。これがないと、将来の有料の絵柄のidを知っていれば、改造アプリで無料で貼れてしまう。有料の絵柄を売るときは、ここを「持っている絵柄なら貼れる」に変える
- ルール側にも絵柄のidの一覧を書く必要があるため、**Dartの一覧とルールの一覧がずれていないかをテストで検証している**（`stamp_design_test`がfirestore.rulesを読んで比較）。絵柄を追加するときは両方に書くこと
- 切手を記録する前に送った手紙や、新しいアプリが送った知らない絵柄の手紙は、**通常切手として表示**する（表示できずに落ちるより良い）
- `sendLetter()`の切手は**省略できない引数**にした。省略すると黙って通常切手になる作りだと、貼り忘れに気づけないため

### ハマりどころ・注意点
- 開封時に手紙を作り直すコードが、**切手のidを引き継がず通常切手に戻してしまう**作りになっていた（項目を1つずつ手で書き写していたため）。`markOpened()`を追加し、項目の書き写しを1か所にまとめて防いだ
- 切手の白い縁がカードの色と同じで、**ミシン目が右と下にしか見えなかった**（影をずらして描いていたため）。四辺に細い縁を描くように直した。実機で確認済み
- 書く画面は縦に長くなり、テストの画面サイズでは「送る」ボタンが最初は作られない（リストが見えている部分しか作らないため）。テストでは`scrollTo()`でスクロールしてから押している

### 検証
- `flutter test` 全120件、`rules_check.dart` 48件（無料の絵柄は貼れる／持っていない絵柄や絵柄なしは拒否）、ルールは本番に反映済み
- エミュレータで、@masaya が桜の切手を貼って送信 → 送った手紙の一覧に桜 → @incomodo 側の未開封の封筒に桜の切手と差出人だけが表示、まで確認。以前の手紙は通常切手で表示された
- **開封後の便箋に切手と消印が重なった状態は、実機では未確認**（開封できる手紙がまだないため、B27と一緒に確認する）

---

## 25. 封筒と便箋も選べるようにする仕組み（P3-7の拡張）

### 目的
作業者の要望：①封筒を一般的な手紙（✉️）の形にしたい、②封筒と便箋も切手と同じ仕組みで選べるようにしたい。**種類は今は用意せず、今後追加できるようにする**。

### 実装内容
- `models/stationery.dart`：切手・封筒・便箋に共通の型`StationeryDesign`と、`EnvelopeDesign`（今は「白い封筒」1種類）、`PaperDesign`（今は「罫線の便箋」1種類）。`StampDesign`もこの型に揃えた
- `theme/stationery_art.dart`：封筒と便箋の見た目（色・罫線の有無）
- `widgets/envelope_view.dart`：✉️型の封筒。ふたの三角と、下の角から伸びる折り目の線を描き、右上に切手、左下に差出人。**最大幅360**（タブレットで巨大にならないように）
- `widgets/design_picker.dart`：切手・封筒・便箋のどれにも使える選択欄。**選択肢が1つしかないときは何も表示しない**
- 手紙を書く画面には封筒と便箋の選択欄もすでに置いてある。**種類を2つ以上にすると自動で表示される**（今は1種類ずつなので出ない）

### 新しい種類を追加する手順（切手・封筒・便箋すべて同じ）
1. `models/`のそのクラスの`free`に1行足す（例：`EnvelopeDesign.free`）
2. `theme/`に見た目を1件足す（切手は`stamp_art.dart`、封筒・便箋は`stationery_art.dart`）
3. `firestore.rules`の`freeStamp` / `freeEnvelope` / `freePaper`のリストにidを足す

3を忘れると`stationery_test`が失敗して気づける。2を忘れると既定の見た目で表示されるが、これも同テストで検出する。

### 判断の理由
- **便箋の種類は本文と同じ場所（`content/body`）に保存する**。封筒と切手は開ける前から見えるものだが、便箋は中身。本文と同じくルールで開封前は読めないので、「開けて初めて便箋が見える」ことが仕組みの上でも守られる
- 封筒の種類は封筒のドキュメントに保存（開ける前から見える）。差出人の控えには3つとも保存
- ルールは切手と同様、**無料の種類以外は受け付けない**。有料の封筒・便箋を作るときに無料で使われないようにするため
- 開封時にデータを作り直す処理（`Letter._copy`）を1か所にまとめ、**項目を増やしても書き写し漏れが起きない**ようにした（24章で切手が消える不具合があったため）

### 検証
- `flutter test` 全131件、`rules_check.dart` 53件（持っていない封筒・便箋、封筒なし・便箋なしは拒否、便箋も開封前は読めない）、ルールは本番に反映済み
- エミュレータで、未開封の手紙が✉️型の封筒（桜の切手付き）で表示されることを確認

### 注意点
- 今回のルール変更で、**封筒と便箋の種類を送らない古いアプリからは手紙を送れない**。現時点ではテスト用のエミュレータだけなので問題ないが、公開後にこうした項目を必須にするときは、古いアプリを考慮すること

---

## 26. 消印に開封した場所を入れる＋手紙のアルバム（B28）

### 目的
要件定義F-03の「消印（開封日・場所）」の**場所**と、開封した手紙を並べて見返す**アルバム**。「その場所まで行って開けた」ことを手紙に残す。

### 実装内容
- 開封するとき、**立っているポストの名前**を`openedPlace`として受取人の封筒に記録する（`LetterService.open(place:)`）
- `widgets/postmark.dart`：消印の上段に場所を入れる（場所がないときは従来どおり「INCOMODO POST」）。長い名前は**8文字で切って「…」**、さらに円に収まるよう縮小する。読み上げは「消印 自宅 2026.9.17」
- 手紙の画面の見出しも「消印：9/17 14:05・自宅」のように場所を出す
- `screens/album_screen.dart`：開封済みの手紙を、**切手に消印が押された姿**でタイル状に並べる。並び順は**開けた日の新しい順**（書かれた日ではなく、取りに行った日）。手紙一覧の右上のアイコンから開く

### 判断の理由
- **場所は受取人の控えにだけ保存し、差出人には見せない**。ポストの名前は利用者が自由につけるもので、「会社」「実家」のように場所が分かってしまう。「送り手は相手のポスト位置を知る必要はない」（要件定義）に合わせた。差出人側の消印は日付のみ。**差出人にも見せたくなったら、ここを変える判断が必要**
- 記録するのは座標ではなく**ポストの名前**。座標は受取人本人にも不要で、漏れたときの害が大きい
- あわせてルールで**開封は1回だけ**にした。これまでは開封済みの手紙の`openedAt`を上書きでき、消印の日付や場所を後から書き換えられた

### 検証
- `flutter test` 全143件（消印に場所が出る・長い名前が収まる・読み上げ・アルバムの並び順と中身・開封で場所が記録される）
- `rules_check.dart` 65件（場所は20文字以内の文字列のみ／差出人の控えには場所を書けない／2回目の開封は拒否）。ルールは本番に反映済み

---

## 27. 手紙を捨てる（B25）

### 目的
手紙が増える一方で消す手段がなかったため、**1通ずつ捨てられる**ようにする。

### 実装内容
- 手紙の画面の右上のごみ箱アイコン →「この手紙を捨てますか？」の確認 → 捨てると一覧に戻る
- 確認文は状況で変える
  - 未開封の手紙：「まだ開けていない手紙です。捨てると、もう読むことはできません。」
  - 開封済みの手紙：「捨てた手紙は元に戻せません。」
  - 送った手紙：「あなたの控えだけが消えます。相手に届いた手紙はそのまま残ります。」
- 届いた手紙は**封筒と本文（`content/body`）を1回のバッチで削除**する。本文だけが残らないようにするため（18章の注意点）

### 判断の理由
- **消せるのは自分の側だけ**。差出人が送った手紙を取り消せると「届いた手紙が、開けに行ったら消えていた」が起きる。ルールでも差出人は受取人の手紙を消せない
- **未開封の手紙も捨てられる**。受け取りたくない相手からの手紙を、ポストまで行かずに処分できるようにするため（ブロック機能ができるまでの逃げ道でもある）
- 使った切手は戻さない（捨てたのは受取人で、差出人は切手を使って送っている）

### ハマりどころ・注意点
- **差出人が控えを捨てた後に受取人が開封すると、開封そのものが失敗していた**。開封は「受取人の封筒」と「差出人の控え」を1回のバッチで更新するが、控えがないとバッチ全体がルールで拒否される（`rules_check`で403を確認）。そこで拒否されたときは**受取人の封筒だけを開封し直す**ようにした（`LetterService.open`）
- ~~開封済みでも`openedAt`を上書きできた~~ → 26章で1回だけに修正

### 検証
- `flutter test`（確認して捨てる／やめると残る／送った手紙は控えだけ／未開封の警告文）
- `rules_check.dart`（差出人は受取人の手紙を消せない／自分の控えは消せる／控えがなくても受取人は開封できる／未開封のまま本文ごと捨てられる）

### 作業環境メモ
- この環境では`firebase`コマンドにPATHが通っていないことがある。**`C:\dev\firebase.exe`を直接使う**（例：`C:\dev\firebase.exe deploy --only firestore:rules --project incomodo-c161e`）
- ルール検証用のエミュレータは、Firebase CLIがなくても**jarを直接起動できる**：`"C:\Program Files\Android\Android Studio\jbr\bin\java.exe" -jar %USERPROFILE%\.cache\firebase\emulators\cloud-firestore-emulator-v1.22.0.jar --host 127.0.0.1 --port 8080 --rules app\firestore.rules --project_id incomodo-c161e --single_project_mode true`

---

## 28. 最初のポストは工事なし（2026/09/17 仕様変更）

### 目的
作業者の判断：**最初に設置するポストは工事時間をなくす**。新しく始めた人が、最初の48時間なにもできずに離れてしまうのを防ぐ。

### 決めたこと
- **アカウントで一番最初に登録したポストだけ**、登録した瞬間から使える
- 2つ目以降のポスト、**移設したポスト**（最初のポストを移設した場合も）は、これまでどおり48時間の工事
- 最初のポストを**削除して登録し直しても、もう工事なしにはならない**（1アカウント1回限り）
- 名前の変更では、工事なしのまま

### 実装内容
- ポストに`firstPost: true`の印をつけ、`Post.activatesAt`は印があれば登録時刻そのものになる
- 「使ったかどうか」は`users/{uid}/meta/firstPost`に記録する。**ポストと同じバッチで作り、ルールで変更・削除を禁止**しているので、ポストを消しても記録は残る
- ホーム画面の説明文と、登録時のメッセージ（「最初のポストなので、すぐに使えます」）を変更

### 判断の理由
- アプリの判断だけにすると、改造アプリで何度でも工事をスキップできる。そのため**ルールで「記録がまだない場合だけ、記録と一緒に作れる」**ようにした（`claimsFirstPost()`）
- 工事期間の目的は「ポストを次々に作り替えて、どこでも手紙を読む」ことの防止。最初の1回だけならこの抜け道にはならない。ただし「ポストを持たないうちに届いた手紙を、今いる場所に最初のポストを置いてすぐ読む」ことはできる。1回限りなので許容した

### 注意点
- この変更より前からポストを持っていたアカウント（テスト用の@incomodo・@masaya）には記録がないため、**ポストを削除して登録し直すと、1回だけ工事なしになる**。公開前なので問題ない

### 検証
- `flutter test` 全144件、`rules_check.dart` 76件（記録なしで工事をスキップ／他人の分を使う／2つ目のポスト／記録の削除／移設で印を残す／印のないポストに印をつける／削除して再登録、がすべて拒否される）。ルールは本番に反映済み

---

## 29. 読み込み中のまま止まらないようにする（2026/09/17）

### 目的
B27の確認中、エミュレータでポストを登録すると**読み込み中の表示が何分も消えず、エラーも出なかった**。原因をはっきり切り分けることはできなかったが（パソコンのメモリ不足でエミュレータ自体が不安定だった）、**どんな原因でも「待ち続ける」ことがない**ようにした。

### 分かったこと
- 位置情報の取得そのもの（`getCurrentPosition`）には、もともと30秒の打ち切りがある（geolocatorの中で`timeout`している）
- 打ち切りがなかったのは、①位置情報がオンか・許可されているかの確認と、②Firestoreの読み書き。**Firestoreの書き込みは、サーバーが受け取ったと返事をするまで完了しない**ため、通信が詰まると永遠に待つ

### 実装内容
- `services/slow_response.dart`：`answerWithin()`（決めた時間で待つのをやめ、案内文つきの`SlowResponseException`を投げる）
- 位置情報：オン・オフと許可の確認を**10秒**で打ち切る（許可を求めるダイアログは利用者の操作待ちなので打ち切らない）
- ポストの登録・名前変更・移設・削除：読み込み**15秒**、書き込み**20秒**で打ち切る
- ホーム画面では、これらの案内文を「失敗しました」をつけずにそのまま表示する

### 判断の理由
- **書き込みを打ち切っても、書き込み自体は取り消されない**（通信が戻ると反映される）。そのため書き込みの案内は「もう一度」ではなく「**少し待ってから一覧を確認してください**」にした。やり直しを促すと二重に登録しようとしてしまう
- ポストの削除・移設の確認文も、最初のポストの仕様（28章）に合わせて出し分けるようにした

### 残っていること（B32）
- **手紙の送信・開封・削除にはまだ打ち切りがない**。送信は「打ち切り後に届く」ことがあり、やり直すと**二重送信・切手の二重消費**になるため、画面の作りから考える必要がある

---

## 30. 古いアプリに更新をお願いする仕組み（B31）

### 目的
公開後にルールで新しい項目を必須にすると（25章の注意点）、更新していない人のアプリは手紙を送れなくなる。そのときに**エラーで動かなくなるのではなく、「更新してください」と案内する**。

### 実装内容
- `models/app_version.dart`：このアプリのビルド番号`AppVersion.build`（pubspec.yamlの`version: 1.0.0+1`の`+`の後ろ）
- サーバーの`config/app`ドキュメントの`minBuild`（数値）より古いビルドなら、アプリ全体の代わりに「アプリを更新してください」の画面を出す（`screens/update_required_screen.dart`の`UpdateGate`）
- ルール：`config`は**誰でも読める（ログイン前でも）、アプリからは書けない**

### 使い方（公開後にルールを厳しくするとき）
1. 新しいアプリを作るとき、pubspec.yamlの`+`の後ろの数字と`AppVersion.build`を**一緒に**1つ増やす（ずれると`app_version_test`が失敗する）
2. 新しいアプリがストアで配信されてから、Firebaseコンソール → Firestore → `config` コレクション → `app` ドキュメントに、`minBuild`（数値）を新しいビルド番号で設定する
3. その後にルールを反映する

### 判断の理由
- **サーバーに届かない・設定がない・読み込み中のときは、止めずに通す**。設定のミスや通信障害で全員がアプリを使えなくなるのを防ぐため
- 追加のパッケージ（package_info_plus）は使わず、番号を定数で持ってテストでpubspec.yamlと照合する形にした。依存を増やさずに済み、ずれもテストで気づける
- ストアへのリンクボタンはまだない。ストアのURLが決まってから追加する

### 検証
- `flutter test` 全153件（ビルド番号とpubspecの一致／古いビルドは案内画面／同じか新しいビルドは通す／サーバーに届かなくても通す／打ち切りの動作）
- `rules_check.dart` 78件（ログインしていなくても読める／アプリからは書き換えられない）。ルールは本番に反映済み

---

## 31. 電波がないときに手紙が二重に送られないようにする（B32）

### 目的
29章で、ポストの登録には「待ちすぎたら打ち切る」仕組みを入れた。手紙の送信・開封・削除にも入れたいが、**送信は打ち切った後にサーバーへ届くことがある**ため、単に打ち切って「送れませんでした」と出すと、利用者がもう一度送り、**手紙が2通届いて切手も2枚減る**。

### 実装内容
- `services/slow_response.dart`に`handOver()`と`StillSendingException`を追加。20秒待っても確認できない書き込みを「**送信中（スマホの中で順番待ち）**」として扱う
- **送信**：手紙を書く画面を閉じ、「電波が戻りしだい、自動で届きます。…もう一度送る必要はありません」と表示する。画面を閉じることで、同じ手紙をもう一度送れないようにした
- **送った手紙の一覧**：サーバーがまだ受け取っていない手紙は「**送信待ち**」と表示する（`Letter.pending`、Firestoreの`hasPendingWrites`）
- **開封**：開封済みとして表示を切り替え（2回目の開封をさせない）、「開封を受け付けました。電波が戻ると、手紙が読めるようになります」と表示する
- **捨てる**：手紙の画面を閉じ、「電波が戻ると、手紙が一覧から消えます」と表示する
- **本文の読み込み**は15秒で打ち切り、「開き直してください」と案内する（読み込みはやり直しても害がない）

### 判断の理由
- Firestoreの書き込みは、**打ち切っても取り消されない**（スマホの中に残り、つながったときに送られる）。そのため「失敗」ではなく「送信中」として画面を進めるのが実態に合う
- 差出人の控えが捨てられていた場合の開封の代替処理（27章）は、**待つのをやめた後でも動く**よう、書き込みそのものにつないだ

### 残っている制約
- 送信待ちの間にアプリを完全に終了すると、書き込みが送られないことがある（Firestoreの仕様）。その場合、送った手紙の一覧から消え、切手も戻る
- 送信待ちの手紙がルールで拒否された場合（切手切れなど）、一覧から静かに消える。今の作りでは理由は表示されない

### 検証
- `flutter test` 全159件（送信待ちで画面が閉じ二重に送れない／開封を2回できない／捨てると画面が閉じる／一覧に「送信待ち」と出る／打ち切りの動作）

---

## 32. 利用規約・コミュニティガイドラインの下書き（B15）

### 目的
アプリ公開・βテストに必須の利用規約を、**今の仕様に合わせて**下書きする。

### 作ったもの
- `terms.html`（LPの`privacy.html`と同じデザイン）。**まだLPからリンクしておらず、Netlifyにも上げていない**
- 冒頭に「**決めていただきたいこと**」をまとめ、本文の該当箇所を赤く示した

### 内容の要点
- 仕様をそのまま書いた：ポストは最大4か所・48時間の工事（最初のポストは1回限りで工事なし）、50m以内でしか開けない、通知なし、友だちにだけ送れる、切手の毎日配布
- **ポストの場所と名前は差出人を含め他の利用者に見せない**ことを明記（26章の判断と一致）
- 送った手紙は取り消せない（相手の手紙は残る）、使った切手は返さない
- 禁止事項に、このサービス特有のものを入れた：**相手のポストの場所を探る、会うことを強要する、位置情報の偽装・アプリの改ざん**
- コミュニティガイドライン：相手の場所を詮索しない／返事を急かさない／会うことを求めない／受け取る人を思い浮かべて書く
- 免責に「**ポストへの移動は自分の責任で、立入禁止の場所や私有地に入らない、歩きながら操作しない**」を入れた（位置を使うアプリ特有のリスク）

### 決める必要があること
1. **対象年齢**（ランダムポストで見知らぬ人とやりとりするため）
2. **有料の切手・デザインの払い戻しとサービス終了時の扱い**（資金決済法の前払式支払手段に当たるかは専門家に確認）
3. **管轄裁判所**
4. 規約の前提になる機能の実装：**アプリ内の通報・ブロック（B33）**、**退会（B34）**。App Storeの審査では、利用者同士がやりとりするアプリに通報・ブロックの手段が求められる

---

## 33. 通報・ブロック（B33）

### 目的
利用者同士がやりとりするアプリには、App Storeの審査で「不適切な内容を通報する手段」と「相手をブロックする手段」が求められる。利用規約の下書き（32章）もこれを前提にしている。

### できること
- **届いた手紙の画面**、**友だち画面の友だち・届いているリクエスト**の「︙」メニューから「通報する」「ブロックする」
- **通報**：理由を5つから選び（嫌がらせ／場所を探られる・会うことを強要される／わいせつ・暴力的・差別的な内容／宣伝・勧誘・出会い目的／その他）、任意で詳しく書いて送る。相手には知らされない。未開封の手紙からも通報できる
- **ブロック**：相手が友だちから外れ、その人からの**手紙と友だちリクエストが届かなくなる**。相手には知らされない。すでに届いた手紙は残る（捨てるのは自分で）
- **友だち画面の「ブロック中」**から解除できる。解除しても友だちには戻らない（リクエストからやり直す）

### データとルール
| パス | 中身 | ルール |
|---|---|---|
| `users/{uid}/blocked/{相手}` | 表示名・ID・日時 | 本人だけが読み書き。自分自身はブロックできない |
| `reports/{id}` | 通報者・相手・理由・詳しく・手紙ID・日時 | ログイン中なら**自分の名前でだけ**作れる。**アプリからは誰も読めない**（通報者本人も）。書き換え不可 |

- ブロックは、自分の友だちリストから相手を外すことで効く（手紙は受取人の友だちリストにいる人しか書けない、18章）。さらにルールで、**ブロックリストにいる人からの手紙と友だちリクエストを拒否**している（`blockedBy()`）

### 運営者がやること（重要）
- **通報はFirebaseコンソール → Firestore → `reports` コレクションで確認する**。今はアプリから通知が来ないので、**定期的に見る必要がある**
- 手紙IDがある通報は、`users/{通報者}/letters/{手紙ID}` と、その下の `content/body` で内容を確認できる（コンソールはルールに関係なく読める）
- 違反があれば、利用規約の第10条に沿って対応する。**アカウントの利用停止の仕組みはまだない**（必要になったらFirebase Authenticationでアカウントを無効化する）
- 通報が来たらメールで知らせる仕組みは、Cloud Functions（Blazeプラン）が必要

### 検証
- `flutter test`：理由のリストとルールの一致／理由を選ばないと送れない／通報内容が正しく送られる／ブロックの説明が出てから実行される／送った手紙には出ない／友だち画面からのブロックと解除
- `rules_check.dart`：他人のブロックリストは書けない／自分はブロックできない／ブロックされた人は手紙もリクエストも送れない／ブロックリストを覗けない／解除すると届く／他人の名前・知らない理由・自分自身の通報は拒否／通報は読めない・書き換えられない

---

## 34. アカウントの削除（退会、B34）

### 目的
App StoreとGoogle Playの審査では、**アプリの中からアカウントを削除できる**ことが求められる。

### できること
- ホーム画面右上のアカウントのアイコン →「アカウントを削除」
- 削除されるもの・残るものを説明した確認のあと、**Googleでもう一度ログイン**してもらい、削除する。終わるとログイン画面に戻る

### 削除するもの・残すもの
| 削除する | 残す |
|---|---|
| プロフィール、ID（他の人が使えるようになる）、ポスト、切手、友だち、届いているリクエスト、ブロックリスト、届いた手紙（本文も）、送った手紙の控え、Googleログインのアカウント | **相手に届けた手紙**（相手の手元に残る。利用規約どおり）、**最初のポストを使ったという記録**（`meta/firstPost`） |

### 判断の理由
- **最初にもう一度ログインしてもらう**。Firebaseは「最近ログインしたアカウント」しか削除できない。データを消した後にそれが分かると、中身のない状態のアカウントが残ってしまうため、先に確かめる
- **プロフィール・ID・切手は最後に、1回の書き込みで消す**。途中で失敗しても、アカウントは使える状態のままで、削除をやり直せる
- **切手だけを消すことはルールで禁止**した（プロフィールと同時の場合だけ消せる）。切手だけ消して作り直すと、何枚でも切手が手に入ってしまうため
- **最初のポストの記録は残す**。同じGoogleアカウントで登録し直すと同じ利用者番号になるため、これを消すと「退会→再登録」で何度でも工事なしのポストが作れてしまう
- 他の人の友だちリストに残る自分の項目は消せない（他人のデータは書き換えられない）。ただし、その人が手紙を送ろうとしても、宛先の友だちリストがないのでルールで拒否される

### 注意点
- **実機ではまだ動作確認していない**（Googleでの再ログインを含む）。B27と一緒に確認する
- 削除する手紙やポストが非常に多い場合、途中で通信が切れると一部だけ消えた状態になる。その場合も、もう一度「アカウントを削除」をすれば残りが消える

### 検証
- `rules_check.dart`：切手だけは消せない／他人のアカウントは消せない／本人ならプロフィール・ID・切手を同時に消せる／最初のポストの記録は消せない
- `flutter test` 全165件

---

## 今後の作業予定（未着手のバックログ、詳細は progress.html 参照）

**2026/09/16、プロフィール・友だち機能（16章）、招待リンク・QRコード（17章）、テキストの手紙の送受信（18章）、ルールの自動検証（19章）、切手システム（20章）、クラフト紙風デザイン（22章）を実装し、2アカウントでの通し確認（21章）を行った**（P3-3B・P3-3C・P3-5・P3-6完了、B22解消）。あわせて**切手のデザインとマネタイズの方針を決め**（23章）、**切手・封筒・便箋のデザインを選ぶ仕組みを実装した**（24・25章、P3-7完了）。Phase 3で残るのは**P3-4（ボイスメッセージ）だけ**。Firebase Storageを使うため、**Blazeプラン（クレジットカード登録）への変更が必要**になる。

- 2026/09/17：**消印の場所とアルバム（B28、26章）、手紙を捨てる機能（B25、27章）を実装。最初のポストは工事なしに仕様変更（28章）**
- 2026/09/17：**読み込み中のまま止まらないようにした（29章）、古いアプリに更新を促す仕組みを実装（B31、30章）**
- 2026/09/17：**電波がないときの手紙の二重送信を防いだ（B32、31章）、利用規約の下書きを作成（B15、32章、`terms.html`）**
- **利用規約で決める必要があること**：対象年齢、有料アイテムの払い戻し、管轄裁判所（32章）
- 2026/09/17：**通報・ブロック（B33、33章）、アカウントの削除（B34、34章）を実装**。どちらも実機では未確認（B27と一緒に確認する）
- **通報はFirebaseコンソールの`reports`を定期的に見る必要がある**（通知の仕組みはBlazeプランが必要）。アカウントの利用停止の仕組みもまだない
- **手紙の「開封」の実機確認（B27）が未完了**。エミュレータがメモリ不足で固まるため、Androidの実機での確認を提案中。確認用アカウントのポストTokyoHomeは削除済み（登録し直すと最初のポストとしてすぐ使える）
- QRコードの読み取り（カメラ）はアプリ内に未実装（B24）。実機テストができる段階で判断する
- 有料切手（アプリ内課金）は未実装（B26）
- 開発PCのメモリ不足でエミュレータが不安定になる問題（B21）。テスト時はビルド用プロセスを終了して運用でしのぐ。改善しなければMac（16GB以上）購入の判断材料にする
- Apple Sign-Inは、iOS対応（Codemagic）に着手するタイミングで追加する
- iOS向けのCodemagicセットアップは、Android版がある程度動くようになった節目で着手する
- B13（開発者アカウント登録）・B14〜B16（商標チェック、利用規約、特定商取引法表記）は、Phase3の間に必要なタイミングで着手する方針（実費が発生する／マネタイズ開始前に対応すればよいものが中心のため、優先度は引き続き中〜低）
