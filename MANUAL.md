# Incomodo 作業マニュアル

## このマニュアルについて

- **最優先の目的**：作業者（人間）が、各作業の「目的」を理解したうえで、同じ作業を再現できること
- **副次的な目的**：AIがこのマニュアルを読んだときに、現在の状態（IDやURLなどの具体的な値）を把握し、次の作業を補助・代行できること
- **更新方針**：新しい作業を行うたびに、このファイルに追記する。過去の記述は基本的に消さず、状態が変わった場合は「現在の値」を更新する
- 進捗の一覧・チェックリストは `progress.html`、作業結果の要約は `README.md` を参照。このファイルは「なぜ・どうやって」を記録する場所

---

## プロジェクトの現在地（要点）

| 項目 | 値 |
|---|---|
| LP公開URL | https://snazzy-parfait-172aba.netlify.app |
| Netlifyデプロイ方法 | 匿名Drop → アカウントにclaim済み → Public設定 |
| 事前登録データの保存先 | Googleフォーム（フォームID: `1FAIpQLSdnMjpCoDwsd4gjBwgpzCEqGPw0SBKWqhOaSx0ZL_3fdaFAXw`） |
| Googleフォームのメール欄entry ID | `entry.50285302` |
| Metaピクセル ID（正） | `1457252082902363`（ビジネスポートフォリオ「incomodo.app」に属する。広告アカウントで選択できるのはこちらのみ） |
| Metaピクセル ID（旧・使用しない） | `1714208769855921`（別の場所で作成されてしまった無関係のピクセル。ビジネス設定のデータセット一覧には出てこない） |
| プライバシーポリシー連絡先 | incomodo.app@gmail.com |
| 運営者名 | 後藤雅也（個人） |
| 需要検証の予算 | ¥10,000 |
| 目標CPA | ¥150〜300（想定登録33〜66件） |
| 需要検証の実績 | ¥10,000で37件登録・CPA約¥270（2026/09/03〜09/10配信） → **Go判定**（詳細は9章） |

---

## 1. LP（ランディングページ）本体の構築

### 目的
Meta広告で需要検証（スモークテスト）を行うための、軽量かつ単一HTMLファイルで完結するLPを作る。広告経由の訪問者の離脱を防ぐため、読み込み速度を優先する。

### やったこと
- `index.html` に、ヒーロー・課題提起・3つの特徴・アプリ画面モックアップ・事前登録フォームを実装
- クラフト紙・アナログをテーマにしたデザインシステム（配色・フォント：Noto Serif JP / Special Elite）
- スクロール連動のフェードインアニメーション、フォーム送信時の疑似モーダル

### 得られた成果物
- `index.html`（LP本体）
- `assets/`（アプリ画面モックアップ画像3枚：shot-map.jpg, shot-letter.jpg, shot-send.jpg）

### ハマりどころ・注意点（コードを触るAI向け）
- **Chromeのレンダリングバグ**：`justify-content: space-between` を使ったflexコンテナ内に、テキストと`<svg>`が兄弟要素として存在すると、SVGが描画されないことがある（headlessでもheadedでも再現）。回避策：`gap`プロパティで間隔を作る、または`margin-left:auto`ではなく単純なflex-startレイアウトにする。**新しくSVGアイコンをflexレイアウト内に追加する際は要注意。**
- 画像を`<img>`タグで使う場合、ファイル名に日本語を使うと将来的なホスティング先によってはURLエンコードの問題が起きうる。現在の`shot-*.jpg`は英数字名にして回避済み。

---

## 2. プライバシーポリシーの作成

### 目的
メールアドレス（個人情報）を収集する以上、個人情報保護法上の観点から必要。また、Meta広告の審査を通すためにも実質的に必須。

### やったこと
- `privacy.html` を新規作成し、LPフッターからリンク
- 運営者情報：法人ではなく個人（後藤雅也）として記載。所在地は「ご請求があれば遅滞なく開示いたします」として、住所は非公開のままでよい形にした（個人情報保護法上、住所公開は必須ではないため）
- Cookie/計測ツールの章に、実際に導入したMetaピクセルを明記

### 判断の理由（AIが将来この方針を変える際に参照）
- **特定商取引法（住所公開が必須）は「有償で物/サービスを売る」場合のみ適用される**。現在の事前登録は無料のため対象外。切手課金（マネタイズ）を開始する時点で別途対応が必要（バックログ B16参照）
- 副業・個人事業主登録なしでも、個人名義でプライバシーポリシーを公開して問題ない

### 得られた成果物
- `privacy.html`

---

## 3. 事前登録データの受け皿（Googleフォーム連携）

### 目的
LPのフォームが送信演出だけで実データを保存していなかった問題を解消する。かつ、これまで作り込んだLP独自のデザイン・モーダル演出を壊したくない。

### 検討した選択肢と判断理由
- **Googleフォーム単体（画面遷移あり）**：無料・データ管理は楽だが、送信時にGoogle標準デザインの画面に遷移してしまい、LPの世界観が崩れる（心理的離脱要因になりうる）
- **Formspree等の外部フォームサービス**：LPの見た目を維持できるが、別アカウント登録・月間送信数制限あり
- **採用した方法：Googleフォーム＋隠しiframe送信**：無料・データはGoogleスプレッドシートに集計されつつ、LPの見た目とモーダル演出を一切変更せずに済む。ユーザー体験上の妥協が最小

### やったこと・再現手順
1. https://forms.google.com で新規フォームを作成し、質問を1つ（メールアドレス、記述式）追加
2. フォームの「送信」→リンクアイコンから viewform URL を取得
3. **entry IDの調べ方**：実際にHTMLを解析しなくても、以下のプレフィルURLをブラウザで開き、入力欄に値が反映されるかで検証できる（実データを送信せずに確認可能）
   ```
   https://docs.google.com/forms/d/e/{フォームID}/viewform?usp=pp_url&entry.{推測した番号}=verify-test-value
   ```
   ページのHTML内に埋め込まれた `FB_PUBLIC_LOAD_DATA_` という変数の中に、質問ごとの数値IDが入っている（`[[質問ID, タイトル, ..., [[entryID, ...]]]]`という構造）。ここから entry ID の候補を見つけ、上記URLで検証するのが確実
4. `index.html` に隠し`<iframe>`と、`entry.{ID}`を持つ隠し`<form target="送信先iframe" action="{フォームID}/formResponse">` を追加
5. 本物のフォーム送信ボタンのJSハンドラで、隠しフォームに値をセットして`.submit()`し、その後に今まで通りモーダルを表示

### 得られた成果物
- `index.html` 内の `#gform-proxy`（隠しフォーム）と `#hidden-submit-frame`（隠しiframe）
- Googleフォームの回答は、フォーム作成者のGoogleアカウントのスプレッドシートに自動集計される

### テスト方法（実データを汚さずに検証したい場合）
- 上記のプレフィルURL方式はGETリクエストなので、実際には送信されない（安全に何度でも試せる）
- 実際にPOSTで送信テストする場合（`.../formResponse`にPOST）は、本物の回答として記録されるので、テスト後にGoogleフォームの回答一覧からテスト行を削除すること

---

## 4. LPの公開（Netlify Drop）

### 目的
広告のリンク先として使える、実際に一般公開されたURLを用意する。

### やったこと・手順
1. https://app.netlify.com/drop を開く
2. `incomodo-lp-deploy.zip`（`index.html` + `privacy.html` + `assets/`一式）をドラッグ＆ドロップ
3. 匿名デプロイの場合、Netlifyの仕様で自動的にパスワード保護がかかる（パスワードはデプロイ直後の画面に表示される。今回は `My-Drop-Site`）
4. 「Claim your site」から無料アカウントを作成し、サイトを紐づける
5. サイト設定でアクセス範囲を **Public** に変更する（「Only team members can view」のままだとログインが必要になってしまう）
6. 更新時は、Netlifyのプロジェクト画面「Production deploys」欄にある「Drag and drop your project folder here」に、更新済みのZIPを再度ドラッグ＆ドロップすればよい（同じサイトに上書きデプロイされる）

### ハマった問題と原因（重要：ZIP作成時の注意）
- **問題**：デプロイ後、画像（`assets/`配下）だけが404になった
- **原因**：Windows PowerShellの `Compress-Archive` コマンドは、サブフォルダを含むZIPを作る際に、フォルダの区切り文字を `\`（バックスラッシュ）で記録してしまうことがある。ZIP規格上は `/`（スラッシュ）が正しく、Netlifyを含む多くのホスティングはこれに従って展開するため、`assets\shot-map.jpg` のような誤ったパスは正しく展開されない
- **対処法**：`Compress-Archive` を使わず、`System.IO.Compression.ZipArchive` を使い、エントリ名を明示的にスラッシュ区切りで指定してZIPを作る。再現コード：
  ```powershell
  Add-Type -AssemblyName System.IO.Compression
  $fs = [System.IO.File]::Open($zipPath, [System.IO.FileMode]::Create)
  $archive = New-Object System.IO.Compression.ZipArchive($fs, [System.IO.Compression.ZipArchiveMode]::Create)
  $entry = $archive.CreateEntry("assets/shot-map.jpg", [System.IO.Compression.CompressionLevel]::Optimal)
  $entryStream = $entry.Open()
  $bytes = [System.IO.File]::ReadAllBytes($localFilePath)
  $entryStream.Write($bytes, 0, $bytes.Length)
  $entryStream.Close()
  # ...他のファイルも同様に追加...
  $archive.Dispose(); $fs.Close()
  ```
- デプロイ後は必ず、`curl`やブラウザで画像URL（例：`{公開URL}/assets/shot-map.jpg`）に直接アクセスし、200が返るか確認すること
- **注意（重複サイトの発生）**：ZIPをNetlify Dropに再アップロードする際、既存サイト（snazzy-parfait-172aba）の「Production deploys」欄ではなく、トップページの新規ドロップ欄にドラッグしてしまうと、別の新しいサイト（例：`clever-gecko-06618d.netlify.app`）として作成されてしまう。ピクセル設置後の試行錯誤中にこれが発生し、同じピクセルコードを含む古いサイトが残ってイベント計測にノイズが入っていた（2026/09/03に発見・削除済み）。**再デプロイ時は、必ず既存サイトのProduction deploys欄にドロップすること**

### 得られた成果物
- 公開URL：https://snazzy-parfait-172aba.netlify.app
- `incomodo-lp-deploy.zip`（常に最新のindex.html/privacy.html/assetsを反映するよう再生成する）

---

## 5. Meta広告の出稿準備

### 5-1. Facebookページ／Instagramビジネスアカウント

#### 目的
Meta広告（Instagram含む）を出稿するには、Facebookページへの紐づけが前提条件になる。

#### やったこと
1. https://www.facebook.com/pages/create でFacebookページ「Incomodo」を作成
2. Instagramで新規アカウントを作成し、「設定→アカウントの種類とツール→プロアカウントに切り替える→ビジネス」から、作成したFacebookページと連携

#### 補足（判断理由）
- 広告出稿に**必須**なのはFacebookページのみ。Instagramアカウントは技術的には無くても広告は出せるが、広告をタップした人がプロフィールを見た際の信頼感のために作成した

### 5-2. Meta Business Manager／広告アカウント・支払い設定

#### 目的
実際に広告費を支払い、配信を行うための課金基盤を整える。

#### やったこと・手順
1. https://business.facebook.com でビジネスアカウント作成（ビジネス名：Incomodo）
2. 「ビジネス設定」→「アカウント」→「ページ」「Instagramアカウント」にそれぞれ追加
3. 「ビジネス設定」→「アカウント」→「広告アカウント」→新規作成
   - タイムゾーン：`(GMT+09:00) 大阪、札幌、東京`
   - 通貨：`日本円（JPY）`
   - **注意：タイムゾーンと通貨は後から変更できない**
4. 「広告マネージャ」→「支払い設定」からクレジットカードを登録

### 5-3. Metaピクセル（コンバージョン計測）の設置

#### 目的
広告経由でLPに来た人が実際に何人登録したかを正確に計測し、CPA（獲得単価）をもとにGo/NoGo判断をするため。これがないと「広告が効いているか」を定量的に判断できない。

#### やったこと・手順
1. https://business.facebook.com/events_manager にアクセス
2. 「データをリンク」→「ウェブ」→「Metaピクセルのみ」を選択
3. ピクセル名：`Incomodo Pixel`、URL：LPの公開URLを入力
4. 設置方法は「コードを自分で追加」を選択し、発行されたピクセルID（`1714208769855921`）を取得
5. `index.html` の `<head>` に、Metaピクセルの基本コード（PageViewを計測）を追加
6. フォーム送信の成功処理（Googleフォームへの隠しiframe送信の直後）に `fbq('track', 'Lead')` を追加し、「登録完了」をコンバージョンイベントとして計測できるようにした
7. `privacy.html` のCookie/解析ツールの章に、Metaピクセル導入の旨を明記
8. LPを再デプロイし、本番URLでピクセル発火・Leadイベントのコードが含まれていることを確認

#### 得られた成果物
- `index.html` に埋め込まれたMetaピクセルコード（ID: `1457252082902363`）とLeadイベント発火処理

#### ハマりどころ：ピクセルが2つできてしまった問題
- **症状**：Events Managerで作った最初のピクセル（`1714208769855921`）は、実際にイベントを受信していた（Events Managerの「データセット」概要画面で確認できた）にもかかわらず、広告キャンペーン作成画面の「データセット」プルダウンには一切出てこなかった
- **原因**：`https://business.facebook.com/events_manager` に直接アクセスして作成したピクセルは、**ビジネスポートフォリオ「incomodo.app」に属さない、別の場所（個人のアカウント寄りの場所）に作られてしまっていた**。広告キャンペーンは特定のビジネスポートフォリオ／広告アカウントに紐づく資産（ピクセル含む）しか選択できないため、選択肢に出てこなかった
- **確認方法**：`https://business.facebook.com/settings` →「データソース」→「データセットとピクセル」を開くと、**そのビジネスポートフォリオに実際に属しているピクセルだけ**が一覧表示される。ここに出てこないピクセルIDは、いくら他の画面でイベントが見えていても、そのビジネスの広告には使えないと判断してよい
- **教訓（AI・作業者共通）**：Metaピクセルを作る際は、Events Managerに直接アクセスするのではなく、**必ず対象のビジネスポートフォリオ／広告アカウントの画面から辿って「データをリンク」する**（本マニュアル5-3の手順1は、実際には広告アカウントの「データセット」選択画面内の「作成」ボタンから行うのがより確実）。作成後は、`business.facebook.com/settings` のデータセット一覧に出てくるかを必ず確認してから、そのIDをLPのコードに使うこと
- 結果として、LPのコードとデプロイZIPを3往復ほど作り直すことになった。同じ間違いを繰り返さないよう、ピクセルIDを変更する際は必ずこのビジネス設定画面で存在確認をしてからコードに反映する
- **解決確認済み（2026/08/31）**：イベントマネージャで「Incomodo Pixel（1457252082902363）」の「概要」タブを見ると、「PageView」5件・「リード」1件がいずれも「アクティブ」状態で記録されていることを確認。ビジネスポートフォリオ配下で正しく計測できている状態になった。ここから広告キャンペーンをゼロから作成する作業に着手

### 5-4. 広告セットの設定：コンバージョンの場所・パフォーマンス目標

#### 目的
「リード」目的のキャンペーンでは、広告セット作成時に「コンバージョンの場所」を選ぶ必要がある。どこでユーザーに登録してもらうかによって、離脱率・計測方法・LPの見せ方が変わるため、選択理由を残しておく。

#### コンバージョンの場所の種類と効果

| 選択肢 | 概要 | メリット | デメリット |
|---|---|---|---|
| **ウェブサイト** | 広告タップ後、自社LPに遷移し、LP上のフォームで登録してもらう | LPのデザイン・世界観（クラフト紙アナログ演出）をそのまま体験してもらえる。ブランド訴求を兼ねられる | 遷移が1ステップ増える分、離脱が起きやすく、一般的にCPA（登録単価）はインスタントフォームより高くなりやすい |
| **インスタントフォーム（旧リード獲得広告）** | Facebook/Instagramのアプリ内で完結するネイティブフォームが開き、その場で登録できる（LPへの遷移なし） | 読み込み待ちがなく完了率が高い。一般的にCPAが下がりやすい。Metaが推奨として提示してくる | LPの世界観・ストーリーテリングを見せられない。フォーム項目もMetaのテンプレートに準ずる |
| アプリ | アプリのインストール・アプリ内アクションを計測する場合に使う | - | 今回はアプリ未リリースのため対象外 |
| Messenger/Instagram DM | メッセージのやり取りを起点にリードを取る | 会話ベースの見込み客育成に向く | 運用の手間が増える。今回のスモークテストの目的（数値でGo/NoGo判断）には過剰 |
| 通話 | 電話発信をコンバージョンとする | BtoB・高単価商材向き | 個人開発の無料事前登録には不向き |

#### 選び方の判断基準
- **LPのデザイン・コピー自体が「不便さを楽しむ」というコンセプトを伝える重要な要素になっている**場合は「ウェブサイト」を選ぶ（コンセプトが刺さるかどうかも同時に検証したいため）
- **とにかく低CPAで「興味を持つ人がどれだけいるか」の母数だけを早く知りたい**場合は「インスタントフォーム」を選ぶ
- 本プロジェクトでは、LPそのものの世界観・ストーリーテリングが訴求の核であり、Go/NoGo判断にはCPAの数字だけでなく「LPを最後まで読んでもらえたか」という質的な手応えも重視したいため、**「ウェブサイト」を採用**

#### やったこと
1. 「コンバージョンの場所」で「ウェブサイト」を選択
2. 「パフォーマンス目標」で「リード数の最大化」を選択（「コンバージョン値の最大化」は、Lead イベントに金額を紐づけていないため選択不可・かつ無意味なので対象外）
3. 「データセット」で「Incomodo Pixel」（1457252082902363）を選択
4. 「コンバージョンイベント」で「リード」を選択

### 5-5. キャンペーンの作成・公開（実施記録）

#### 目的
5-1〜5-4で整えた土台（ページ・広告アカウント・ピクセル・コンバージョン設定の考え方）を使って、実際にキャンペーンを作成し公開する。

#### 実際に設定した内容（2026/09/03 公開）
| 項目 | 値 |
|---|---|
| キャンペーン目的 | リード |
| 予算戦略 | キャンペーンの予算（Advantageキャンペーン予算オン）／通算予算 ¥10,000 |
| キャンペーン入札戦略 | 最大数量 |
| 広告スケジュール | 2026/09/03〜2026/09/10（7日間） |
| コンバージョンの場所 | ウェブサイト |
| パフォーマンス目標 | リード数の最大化 |
| データセット／イベント | Incomodo Pixel（1457252082902363）／リード |
| 地域／年齢 | 日本／20〜35歳（Advantage+オーディエンスにより上限は自動拡張されうる） |
| 詳細ターゲット設定 | 本（Books）・文学（Literature）・文筆（Writing） |
| 配置 | Advantage+配置（自動） |
| クリエイティブ | `ads/ad-feed-square-1080x1080.jpg`（手動アップロード、シングル画像） |
| リンク先URL | https://snazzy-parfait-172aba.netlify.app |
| 発見リンク | 使用しない（サイトと無関係な商品リンクが候補に出るため、チェックを入れずスキップ） |

#### 手順上の注意点（AI・作業者共通）
- **予算のスケジュール指定は広告セット単位**：キャンペーンレベルの「予算」設定画面には開始日・終了日の入力欄がない。「広告スケジュール」で「スケジュールを設定して広告を配信」を選ぶと、実際の期間指定は広告セットの設定画面に出てくる
- **「発見リンク」機能に注意**：クリエイティブ設定時に「発見リンクを管理」という項目が出ることがあり、自社サイトと無関係な商品（他の広告主のものと思われる画像）が候補として表示されることがある。「広告掲載中に、関連する発見リンクを特定して追加する」のチェックは入れずにスキップしてよい
- **URLパラメーターは現状不要**：LP側にGoogleアナリティクス等の解析ツールを導入していないため、トラッキング設定の「URLパラメーター」欄は空欄のままで問題ない
- 年齢の上限が選択できないのは、Advantage+オーディエンスが標準になっているため（下限のみ指定し、Metaが自動で範囲を拡張する仕様）

#### 得られた成果物
- Meta広告キャンペーン（目的：リード、予算¥10,000、期間2026/09/03〜09/10）が公開済み
- 結果の確認・Go/NoGo判定は本マニュアル6章の基準に沿って、配信終了後（2026/09/10以降）に行う

---

## 5-6. 事前登録者への自動サンクスメール＋ヒアリング（Google Apps Script）

### 目的
- 登録直後にお礼メールを送ることで、登録者に安心感・信頼感を持ってもらう
- 単なるお礼で終わらせず、返信という形で定性的なユーザーの声（既存SNSへの不満点）を集め、Go/NoGo判断やLP・プロダクトの改善材料にする（数値だけのCPA判断を補強する狙い）

### やったこと
- Googleフォームに紐づくApps Scriptプロジェクトに、フォーム送信をトリガーとする`onFormSubmit(e)`関数を実装
- フォームの質問項目「メールアドレス」の回答を`e.response.getItemResponses()`から質問タイトルで検索して取得し、`MailApp.sendEmail()`で送信
- インストーラブルトリガー（フォームから／フォーム送信時）を設定し、送信のたびに自動実行されるようにした

### 判断の理由
- スプレッドシート側の列番号や順序に依存する実装（`e.namedValues['メールアドレス']`等）ではなく、フォーム自体のアイテム（質問）タイトルから検索する実装にした。理由：将来フォームに質問を追加・並び替えしても、質問文「メールアドレス」さえ変えなければ壊れないため
- メール本文に、開発の背景（個人開発・スローコミュニケーションというコンセプト）と、返信を促すヒアリング文言を含めることで、事前登録という受動的なアクションを、能動的なフィードバックのきっかけに変える設計にした
- 署名は「Incomodo開発者　後藤雅也」と実名を明記。プライバシーポリシーで既に運営者名を公開しているため、実名を出すデメリットはなく、個人開発者が直接返信を読んでいるという信頼感がヒアリングへの返信率を高めると判断した

### 得られた成果物
- フォーム紐づけのApps Scriptプロジェクト内の`onFormSubmit`関数
- インストーラブルトリガー（フォーム送信時に自動実行）

### 今後の運用時の注意点
- 返信メールの宛先は、送信元Gmailアカウント（フォーム作成者のアカウント）に届く。返信内容は都度目視で確認し、有用な意見はMANUAL.mdやprogress.htmlのバックログに反映する
- メール文面を変更したい場合は、Apps Scriptエディタの`body`変数を直接書き換えて保存するだけでよい（トリガーの再設定は不要）

### 過去registrant（トリガー設定前の登録者）への一括送信
- **目的**：`onFormSubmit`トリガーは設定後の新規登録にしか効かないため、それ以前に登録してくれた人には自動でメールが届かない。取りこぼしなく同じお礼＋ヒアリングメールを届けるため、一括送信用の関数を別途用意した
- **実装**：同じスクリプトファイルに`sendThankYouToPastResponses()`関数を追加。`form.getResponses()`で全回答を取得し、既に自動送信済み（トリガー設定後に登録され、`onFormSubmit`で個別に送信済み）のメールアドレスを`ALREADY_SENT`配列で除外してから、残りの回答者に同文面のメールを送信する
- **二重送信防止の考え方**：トリガー設定後に届いた新規登録分（テスト送信1件＋実登録2件）のメールアドレスを`ALREADY_SENT`にあらかじめ手動で入れておくことで、それらをスキップする設計にした
- **実行結果（2026/09/06）**：8件に一括送信完了
- **再実行する際の注意**：この関数は「送信済みリストで自動的に重複を防ぐ」仕組みではなく、`ALREADY_SENT`の手動リストに頼っている。将来また過去分をまとめて送りたくなった場合は、その時点までに自動送信済みのアドレスを都度`ALREADY_SENT`に追記してから実行すること

---

## 6. 需要検証の目標設定（予算・KPI・Go/NoGoの基準）

### 目的
「いくら使ったら、どんな結果ならどう判断するか」を事前に決めておくことで、広告配信後に感情的・場当たり的な判断をしないようにする。

### 決定内容
| 項目 | 内容 |
|---|---|
| テスト予算 | ¥10,000（副業として無理のない範囲） |
| 目標CPA | ¥150〜300 |
| 想定登録件数 | 予算÷CPAで 約33〜66件 |
| テスト期間 | 最低5〜7日間（Metaの配信最適化には数日かかるため） |
| **Go基準** | 30件以上の登録、かつCPAが¥300以内。または件数が少なくても明確な反応（保存・シェア・コメント等）がある場合 |
| **要見直し基準** | クリック（LP来訪）はあるが登録に至らない → LP・コピーの問題の可能性。文面やターゲティングを調整して再テスト |
| **No-Go基準** | CPAが¥500を大きく超える、またはクリック自体が少なく反応が薄い |

### 判断の理由
¥10,000という金額は統計的に厳密なCPAを出すには少ないため、**数字の精緻さより「手応えがあるかどうか」の肌感覚判断**という位置づけにしている。

---

## 7. 進捗管理の運用（progress.html）

### 目的
やるべきタスク・課題を可視化し、番号で参照しやすくすることで、指示のやり取りを簡潔にする。

### ルール
- ロードマップの各タスクには `P{フェーズ番号}-{通し番号}`（例：`P1-1`）を付与
- バックログの各課題には `B{通し番号}`（例：`B1`）を付与（現在 `B1`〜`B17`）
- 番号は、各項目のチェックボックスの`id`属性（`p1-1`, `i1`など）から自動生成される（`progress.html`内の`injectItemCodes()`関数）。新しい項目を追加する際は、既存の命名規則（`pフェーズ-連番` / `i連番`）に沿ったidを振れば自動的にバッジが付く
- チェック状態はブラウザの`localStorage`に保存される（サーバー保存ではないため、別ブラウザ・別端末では引き継がれない点に注意）

---

## 8. 広告クリエイティブの制作

### 目的
LP自体とは別に、Instagram/Facebookのフィード・ストーリーズに実際に表示される広告画像とコピーが必要。

### 判断の理由
- 画像生成AIに頼らず、LPと同じHTML/CSSの技術で作成した。理由：LPの既存デザイントークン（配色・フォント・postmarkモチーフ）とアプリ画面モックアップ画像（`assets/shot-*.jpg`）をそのまま流用でき、ブランドの一貫性が保てるため
- 制作方法：指定サイズ（例：1080×1080）ちょうどのHTMLファイルを作り、headless Chromeで`--window-size`をそのサイズに指定してスクリーンショットを撮ることで、正確なピクセルサイズの広告画像を書き出した

### やったこと
1. フィード用（正方形 1080×1080）：`shot-map.jpg`を主役に、見出し・CTAバッジを配置
2. ストーリーズ用（縦長 1080×1920）：`shot-letter.jpg`を主役に、同様のレイアウトを縦向けに再構成
3. 広告文面（プライマリテキスト／見出し／説明文）をLPの既存コピーのトーンに合わせて作成

### ハマりどころ
- 最初、スマホモックアップ画像を大きくしすぎて見出しテキストと重なった。**広告クリエイティブは要素同士が重ならないよう、画像サイズと文字の縦position を先に紙面全体でレイアウト計算してから実装するとよい**（1080×1080なら、上から：eyebrow 60-100px / 画像 120-770px / 見出し 790-950px / CTA 950-1040px、のように帯で区切って設計した）

### 得られた成果物
- `ads/ad-feed-square-1080x1080.jpg`
- `ads/ad-story-1080x1920.jpg`
- 広告コピー（プライマリテキスト・見出し・説明文・CTAボタン文言「今すぐ登録」）

---

## 9. 需要検証の結果とGo判定

### 目的
2026/09/03〜09/10に配信したMeta広告の実績を集計し、6章で定めた基準に沿って本格開発（Phase3）に進むか判定する。

### 実績
| 項目 | 値 |
|---|---|
| 消化金額 | ¥10,000 |
| 登録件数 | 37件 |
| CPA | ¥10,000 ÷ 37 ≈ ¥270 |

### 判定
6章のGo基準（**30件以上の登録、かつCPAが¥300以内**）を両方クリアしたため、**Go判定**（2026-09-10）。想定登録件数レンジ（33〜66件）にも収まっている。

### やったこと
- `progress.html` の P2-4（CPA計測・分析）・P2-5（Go/NoGo判定）にチェックを入れ、Phase 2を完了扱いに更新
- 統計タイル・バックログB7の記述に実績値を反映

### 得られた成果物
- 更新済みの `progress.html`（Phase 2完了、全体進捗50%）

---

## 今後の作業予定（未着手のバックログ、詳細は progress.html 参照）

**2026/09/10、需要検証がGo判定となり、Phase 3（MVP開発）着手の段階に入った**（9章参照）。

- 次のタスクはP3-1（開発環境構築：Flutter + Firebase）から
- B13（開発者アカウント登録）・B14〜B16（商標チェック、利用規約、特定商取引法表記）は、Phase3の間に必要なタイミングで着手する方針（実費が発生する／マネタイズ開始前に対応すればよいものが中心のため、優先度は引き続き中〜低）
